## 2026-09-03：架构文档必须写现行路径，不要把快照当正式前端

- 读者界面是 Vue + API + `content_editorial_zh` 卡片，不是 `home.json` / `navigate-home.v1`。
- Cloudflare Vite plugin、Wrangler、OpenAI Sites Worker 和 `frontend/server` 不是 Docker 部署的一部分；Docker 走 nginx 静态资源。
- 迭代 crawl report、美妆 HTML 日报基线和空的根目录 `data/navigate.db` 不是现行交付物。
- 本地 `.venv` 的 console script shebang 可能仍指向旧目录 `Navigate/`。`uv run pytest` 会用错解释器；用 `uv sync --reinstall` 或 `python -m pytest`。
- 重建 `web` 后必须重建 `cloudflared`，因为它用 `network_mode: service:web`。

## 2026-09-03：简报栏目不是卡片标签

- 导语要覆盖本期条目，不能只抄第一篇。
- `星尘智能`/`机器人` 是实体或主题词，不是栏目。栏目用融资、人事、监管、产品、要闻。

## 2026-09-03：简报报头不是卡片全文

- `daily_lead.text` 是报头导语，只能放头条第一句。
- 卡片三段摘要留给条目正文，不要 join 后塞进 masthead。

## 2026-09-03：简报按主题筛稿，文案复用通用卡片

- 每日简报不要为每个主题再写一遍 `topic_content_editorial`。卡片已经过 LLM。
- 主题分类留给 `TopicMatch`；简报只排版已匹配稿的 `content_editorial_zh`。
- 打开简报是读路径，不要在 GET 上触发生成。

## 2026-09-03：中文卡片不要被标签键和万级换算整篇打掉

- 读者卡片 tags 是可选的。`tag_key` 写成「男士护发」应丢掉该标签，不要让整篇重试后开始编数字。
- `70,000` 对应中文「7万」。只认 million/billion 词，万级原文数字过不了校验。

## 2026-09-03：展示中的稿必须全部走卡片 LLM，不能抽样

- 探索/主题里能看见的条目，入库后都要生成 `content_editorial_zh`，不要每源只打 12 篇留下抽取式残稿。
- 缺编辑件判定要看当前 schema 和 validator；只比 content_hash 会让旧卡片永远占着，管道升级后不会重打。
- 存量重打用独立脚本一次跑完，爬虫每分钟回填 4 篇只收漏网。日限 100、每批 2、不设 max_tokens 仍然有效。

## 2026-09-03：DeepSeek 卡片要限证据和日配额，不要限输出长度

- 一次请求几篇不是最大成本；把 8 万字正文整段送进去才是。卡片证据用标题 + 摘要窗口 + 正文前 2500 字。
- 请求 2 篇、抓取 12、回填 4/分钟、北京时间日限 100。到顶跳过，不要阻断采集。
- 不要设 `max_tokens`。JSON 卡片被截断会整批失败，比多花一点输出更贵。

## 2026-09-03：卡片成品用 LLM，网页壳仍用规则

- 标准化格式、摘要、英译中是编辑任务，不能靠抽取句子。LLM 只看洗净后的 evidence，不要让它清洗 HTML。
- 通用卡片走 `content_editorial_zh`，按 `content_hash` 缓存；不要每个主题把同一篇重写一遍。
- 抓取分发后生成，爬虫轮询再回填存量。缺 key 或失败必须跳过，采集不能停。
- 证据文本变了要升 `content-evidence` 版本，否则缓存会继续引用带署名的原文。

## 2026-09-03：署名是导语前缀，不是导航行

- 36氪把 `图源/企业 本文约 3300 字，建议阅读 8 分钟 作者丨欧雪 编辑丨袁斯来` 和导语粘在同一句里，中间没有 `。`。
- 只按行丢导航/广告不够；要在分句前反复删这些块，再取卡片句子。
- 不要全局禁「作者」「来源」「设计」：`作者认为`、`设计：简约风格` 是正文。只匹配 `作者丨姓名`、`来源：站点` 这类结构。
- 清理残留分隔符时不要把正文句末 `。` 一起剥掉，否则编辑摘要会缺句号。

## 2026-09-03：卡片抽取前必须先清网页壳

- 腾讯/36氪正文常是 Markdown：导航链接、广告图、`ck=`/`sha256` 追踪段。先按行丢掉壳，再抽句子。
- 不能把全文压成一行再按句号切，导航没有句号，会整段变成卡片第一句。
- 可用摘要不要因为略长于 280 字就丢掉；标题重复前缀要剥掉。
- 「广告」不能当全局禁词，正文里的「广告技术」是合法事实。

## 2026-09-03：事件合并靠实体签名，不要切阈值

- 中英同一事件的字 3-gram 几乎不相交，把 `auto_match_threshold` 降到能合并的程度会把同主题不同公司绑在一起。
- 先清洗标题尾巴和繁简，再抽公司名/产品名；`具身智能`、`人工智能`、`融资` 只能当动作或主题，不能当实体。
- 金额要用单位归一。`2.9` 和 `$2.9 billion`、`20亿美元` 和 `two billion` 不是冲突数字。
- 中文不要对整句做滑动 3-gram：主题套话会被切成 `身智能` 这种假共享。

## 2026-09-03：站内卡片读摘要，不把标题做成外链

- `content_editorial_zh` 的 `chinese_summary` 是 Python 属性，库里只有 `summary_units`。读者层要自己拼接。
- 没有编辑件时，用正文前几句抽取，并去掉 `首页 > 资讯 > 正文` 这类面包屑。
- 列表和弹层都先给凝练段落；原文链接放在次入口，避免点标题就离开平台。

## 2026-09-03：采集列表删除必须包含爬不了的源

- 勾选框如果绑定 `sourceRunnable`，失败/熔断/封禁的站就永远选不中，删除所选也无从谈起。
- 有抓取历史的源不能物理删（内容外键 RESTRICT）；停用并移出网站列表，已取回的稿保留。

## 2026-09-03：今日事件只放跨来源聚类

- 前台「今日事件」不是把每篇稿再列一遍。没有 2 个以上可读成员的 Event 必须留在精选列表，不能占据事件栏。
- Vue 的 `:href` 不能直接绑 `string | null`；过滤后 TypeScript 也不会收窄，要用 `url ?? undefined`。
- `POST /api/v1/topics` 成功码是 201，不是 200。

## 2026-09-02：中英关键词要覆盖写法变体，不能只留整句

- 中文复合意图（「具身智能创业融资」）必须拆出「具身智能」「创业融资」，否则标题里分开出现就会漏掉。
- 中英夹杂时不要把被英文切开的 2 字碎片（「全球」「资讯」）当成关键词，否则会误伤所有带「全球」的稿。
- 目录里有繁体源和英文源：匹配前要繁简折叠；英文要词边界加简单复数，排除词也要认 `exclude`。
- 已在库里的可读稿也要回填主题，不能只等下一次抓取。
- 匹配字段只能用 positive_keywords 和 query_expansions。LLM 的 event_types（融资/发布）和自作主张的 excluded_keywords（招聘/jobs）会把正文页脚误伤成排除或误伤成命中。
- 「AI」这类两字母英文只看标题；摘要、标签、正文里的 AI 太容易误伤。
- 行业中英别名要落在区分性概念上：原料可以对应 ingredient/actives，美妆不能对应 beauty，否则零售稿会淹没原料主题。

# 项目经验

## 2026-09-02：目录稿要双语匹配后分发给所有订阅

- 网站抓取只入库不够；抓完必须把可读新稿匹配到所有 active 主题，不能等用户下次预览主题。
- 中英源并存时，关键词不能只做裸子串：英文要词边界和 `K-beauty`/`K Beauty` 变体，中文仍用子串，混合意图两种都保留。
- 纯中文主题对英文稿不要直接 exclude；没有 expansions 时标 review，交给 LLM 相关判断。有 query expansions 时先走关键词命中。
- LLM 不能按「每篇文章 × 每个主题」乱打。先编译主题的双语 expansions，再只处理 review 批次。

## 2026-09-02：目录抓取的去处是探索，不是静态快照

- 「探索」必须读当前库里的网站来源内容，不能继续用 `home.json` 美妆快照。
- 只有有发布日期且正文可读的条目进入探索；主题发现用的 `user_topic` 源要排除。
- 手动抓取完成后要刷新探索列表，操作者才能在前台看见刚取回的稿。

## 2026-09-02：手动抓取必须真的取内容，并让操作者看见结果

- 目录源 `max_articles=1` 只适合调试。首页第一条入库后，再点抓取只会 skipped=1，看起来像“没抓到”。
- 临时抓取要等 run 结束再展示取回/新增；不能只丢 BackgroundTask 然后立刻刷新列表。
- 已有内容的源日常窗口是 1 天。手动抓取需要独立的 7 天窗口，否则列表里的近期稿会被时间窗拒掉。
- 网站管理列表不能把长网址和路径标签放在同一行挤在一起；名称、网址、路径要分行。

## 2026-09-02：管理员接口先鉴权再校验载荷

- 来源探测、注册、列表和调度一旦改为登录 + 管理员权限，原有“空库校验 422 / 空列表”测试必须先创建并登录管理员。
- 未登录请求会在 Pydantic 校验前返回 401，不能再用匿名客户端断言密钥配置 422 或 `GET /sources == []`。
- Docker 只挂载 `backend/data`，应用代码在镜像里；后端新增模块后必须重建 `backend`/`crawler`，不能只重启 `web`。

## 2026-09-02：目录来源的日常采集是北京时间槽位，不是间隔轮询

- 现有网站列表要每天固定两次：09:30 和 18:00（Asia/Shanghai）。到期判断必须按槽位，而不是 `fetch_interval_seconds`。
- 每个槽位每个启用源只派发一次；失败由熔断冷却约束，不能在同一分钟里把站点打穿。
- 日常调度只走五种执行引擎。Firecrawl 只用于主题 Search/硬页 Scrape，不能变成第六个每日爬虫。
- Docker 里 API 进程不会自己爬；必须有独立 crawler 服务轮询到期源，且与 API 共享同一数据库。


## 2026-09-02：Firecrawl Search 之后先走廉价正文，Scrape 只做硬页兜底

- Search 只负责发现 URL 和去重；完整池内容直接复用，不得为同一规范 URL 再扣 Scrape 额度。
- 同主机若已有启用中的 RSS/API/Sitemap/HTML 源，只用该引擎的详情解析，禁止对每个 Search URL 跑五种列表巡检或整站 Probe。
- 通用 HTML 拿到发布日期且正文可用时，必须跳过 Firecrawl Scrape；403、JS 空壳、无日期或正文过短才允许 Scrape。
- 冷却只约束付费 Scrape 重试，不能把“本地解析失败”当成可以反复扣费的理由。


## 2026-09-02：主题日报必须是一期纸面，不是内容清单

- 刊头用主题名，导语用头条事实，栏目按标签或要闻/续闻分栏；禁止“本期资讯”一栏倒完。
- 工作台默认是阅读面；额度、待补全、发现来源只出现在主题管理，不占据精选和日报入口。
- 日报强调色必须与品牌酒红 `#722F37` 同一套，不能再出现另一套粉红强调色。

## 2026-09-02：读者界面不要解释系统如何工作

- 前台和日报只放结果，不放流程说明；额度何时消耗、是否查询共享池、覆盖了哪一天的发布内容，都属于内部机制。
- 日报导语必须是本期事实，不能写成“本期整理 N 条与某主题相关的已发布资讯”这类自我盘点。
- 空状态用最短判断（暂无匹配），不要教用户去哪个按钮完成内部步骤。

## 2026-09-02：时间窗必须是进入用户池之前的硬门禁

- 搜索供应商的日期参数只是召回提示，不能当作发布日期保证；候选必须经过页面日期识别，再以明确的窗口边界决定是否进入主题匹配和读者流。
- `discovered_at` 只能描述发现时间，绝不能在发布日期缺失时替代 `published_at` 通过首次 7 天或增量 1 天门禁。
- 对解析出窗口外发布日期的内容，可以保留在全局原始池中供审计和去重，但不得建立当前主题匹配；日期仍缺失的内容继续待补全，也不得进入日报。

## 2026-09-02：主题生命周期能力必须包含安全删除

- 创建、暂停不构成完整的订阅生命周期；用户必须能删除自己不再需要的主题。
- 删除主题应由服务端校验所有权并依赖明确的外键级联；前端确认只防误触，不是权限边界。
- 删除当前选中主题后，必须同步清除选中态、日报历史和候选来源，避免界面继续引用已不存在的对象。

## 2026-09-02：账户体系必须匹配产品的最小登录心智

- 面向内部或早期订阅产品时，登录表单不应强迫用户提供邮箱、显示名等非认证必需字段；账户名与密码应是独立且明确的首要字段。
- 首次管理员引导、普通用户创建和管理员账号管理是三个不同的产品状态：后两者不能被塞进同一个账户弹窗，管理员能力应有独立工作台入口。
- 用户要求“清库重来”时，必须先定位运行时实际使用的数据库文件；在确认后优先做带日期的可恢复归档，再新建空库和迁移验证，绝不能误清理同名的闲置文件。

## 2026-09-02：正文存在不等于主题发现内容已可用

- Firecrawl Search/Scrape 的 Markdown 可能没有 `published_at`；即使正文很长，也必须按统一质量门禁视为待补全，不能只以 `metadata_only` 判断是否需要重试。
- 所有入池入口都必须接入同一份“缺日期或缺正文 → 合规网页解析 → 快照 → 内容契约合并 → 冷却”流程；API 回退已实现不代表主题发现分支天然继承。
- 对已有不完整内容，先复用其规范 URL 进行网页补全；不要为了补日期再次调用付费发现/抓取供应商。

## 2026-09-02：发布日期解析必须是可审计的多信号降级

- 不能只读取单个 JSON-LD 字段。应按高可信发布元数据、语义属性、URL、正文邻近文本的顺序尝试，并记录成功信号来源。
- 网页正文解析失败不代表日期不可用；日期提取应可独立运行，但只补能被页面或 URL 明确支持的日期。
- HTTP 403/521、搜索页和无日期的常青内容不是“解析失败后可以猜”的对象；保留空值，交给质量门禁和后续人工/授权通道处理。

## 2026-09-02：首次体验与后台工作台必须分层

- 未登录用户不应先看到内容工作台或访客情报流；登录是个性化订阅的明确边界，首次无主题状态应直接引导用户表达兴趣。
- 自动首次采集必须让用户看见进展、失败和完成状态；阶段型进度可作为首版体验，但后续若采集耗时增长，应升级为服务端可轮询的逐项进度。
- 管理员账号能力必须在服务端进行角色判定，前端隐藏按钮只改善体验，不能充当权限边界。

## 2026-09-02：统一契约不等于读者可用质量

- `article.v1.1` 允许发布日期和正文完整度为空，是为了不丢失候选，不代表这些内容可进入日报或读者流。
- 必须把“完整解析、发布日期可审计、部分内容、仅元数据”明确分层；前台默认只能消费有发布日期且正文可用的档位。
- 去重不能只依赖内容入库后的唯一键；付费抓取前还要复用全局规范 URL，并对失败的补抓建立跨主题冷却，避免重复扣费。

## 2026-09-02：结构化 LLM 输出需要严格契约与无语义适配层

- Prompt 中必须给出精确字段形状，但不能假设 Provider 永远遵守字段名；常见同义字段应在校验前做白名单映射，禁止对事实值做猜测或改写。
- 相关布尔值到排序分等确定性投影必须显式记录，不能伪装成模型原始分数；标题、摘要、证据和相关判断仍要通过严格校验。
- 校验失败和修复请求也会消耗 tokens；所有请求状态和 usage 都应在调用后立即持久化，不能只记录成功工件。

## 2026-09-02：订阅对象应是用户兴趣意图，不是平台预设领域

- “用户订阅”不能默认解释成订阅 beauty 等固定频道；面向通用资讯平台时，核心对象应是用户创建的主题，输入可以是关键词或自然语言描述。
- `Domain` 适合平台内部治理、分类和编辑策略；`TopicSubscription` 才适合表达用户个性化需求。两者可以关联，但不能共用同一个数据实体。
- 外部抓取服务只负责发现和获取候选内容，不能直接决定订阅命中；命中仍需经过标准化、去重、时间窗和可审计的主题匹配。

## 2026-08-31：读者前台不能暴露内部评分与流程说明

- 价值分、算法排序口径、聚类证据说明和编辑工作流属于内部审计信息，不能默认占据资讯读者界面。
- 面向中文读者的产品视图必须以中文编辑工件为准；英文标题或英文摘要缺少合格中文工件时，不得直接回退到前台展示原文。
- “来源属于关注清单”不等于“该来源每篇文章都属于当前行业”。只有明确的垂直行业来源可以来源级准入，综合来源必须逐篇通过领域证据判断。
- 领域准入策略必须放在版本化领域配置中，不能把美妆关键词写死在通用处理器，也不能用 `dedicated` 掩盖综合来源的内容差异。

## 2026-08-31：发布目标必须与用户的访问边界一致

- 用户要求先使用本地版本时，不得默认把前端发布到 ChatGPT Site；即使部署成功，账号级访问控制也可能让用户当前浏览环境无法访问。
- 交付前应区分本机预览、局域网可访问服务和公网托管，并验证用户实际需要的访问方式；没有明确授权时先提供本地生产预览。
- 用户明确撤回某个托管目标后，后续默认交付不得继续引用该地址；重新公开发布必须另行确认平台和访问策略。

## 2026-08-30：用户指定的前端框架是硬约束

- 用户明确要求 TypeScript + Vue 时，不能因为现有脚手架或托管模板默认使用 React/Next 就替换技术栈。
- 开工前必须核对 `package.json`、入口组件和构建配置是否与指定框架一致；交付说明也必须准确写明实际技术栈。
- 迁移框架时保留已经确认的数据契约、视觉和交互，只替换实现层，并重新完成类型检查、生产构建、页面入口和发布链路验证。

## 2026-08-29：历史文章可访问不等于网站仍在持续发布

- 搜索引擎结果和旧文章页只能证明归档可访问，不能证明来源仍有当前发布能力。
- 恢复停用来源前必须读取根域公告，并核对最新文章发布日期是否晚于关闭/迁移时间；两项证据冲突时以发布方当前公告和实际发布序列为准。
- 已关闭站点的替代媒体必须创建新的来源身份和独立验收，不能把继任栏目 URL 填进旧来源后假装连续。

## 2026-08-29：不能用旧入口或一次维护页判定整个来源失效

- 入口 404/410 先检查根域、栏目导航、当前文章 URL、官方迁移公告和 Feed/Sitemap；旧路径失效不等于网站关闭。
- 维护页、WAF 挑战、robots 明确禁止、网络不可达和解析规则错误必须分别建模，并带观测时间和复测期限。
- 配置写着 `browser` 不代表浏览器能力已经实现或验收；能力声明必须能映射到真实执行引擎和成功运行证据。
- robots 的 4xx、5xx/网络错误和 200 `Disallow` 语义不同；即使采用更保守的内部策略，也不能在审计数据中混称为“robots 禁止”。

## 2026-08-29：日期型采集的覆盖日必须冻结在运行记录上

- “永远抓前一天”是首次任务创建规则，不是每次执行时读取当前时间；否则排队、跨日重试和历史补采都会抓错日期。
- `coverage_date`、发布时区快照和重试溯源属于 CrawlRun 契约。执行器只消费冻结上下文，来源配置变化不能改写已创建任务。
- 同源活动任务只有在覆盖日、时区和重试来源完全一致时才能幂等复用；上下文不同必须返回冲突，不能悄悄借用另一个任务。
- 历史无偏移时间应按已知来源时区本地化后转 UTC。修复必须保留旧 Raw、追加修正版、原子更新最新投影，并以候选数量保护和第二次命中 0 证明幂等。
- 显式历史覆盖日的日报出版日应由覆盖日加一天确定，不能跟随重建当天漂移；故事集合缩小时，可复用已有单篇工件和既有栏目位置离线裁剪，不为修复重复消耗模型额度。

## 2026-08-28：日报采集必须按来源发布日期选取，不能按列表位置猜测

- 日报型来源的正式采集条件应是“来源时区内前一自然日”，不能用置顶、广告、第一篇、第二篇或列表顺位代替发布日期。
- 第三方时间字段没有时区时，必须按来源明确配置的时区解释，再统一保存为 UTC；不能先假定 UTC 再在日报层加时区。
- 日期筛选必须验证分页已经越过目标日期边界；只看第一页或达到本轮数量上限就停止，会在高频发布日静默漏采。
- 目标日无内容是健康空结果；分页无法闭合、时间字段不可解析或排序不可信才是失败。
- 发布日期是硬筛选，但不等于必须收录所有命中项：若上游提供明确的置顶或广告布尔字段、类型或标签，可以在日期命中后排除；不能用标题关键词或列表顺位推断。

## 2026-08-28：实验成功、历史导入和生产渠道必须分开

- 能进入统一数据契约，只能证明格式兼容，不能证明该采集方式具备持续发现、增量同步和稳定调度能力。
- 浏览器单篇验证、一次性离线归档、参考项目都不能列入正式渠道能力；只有通过多周期验收的执行引擎才能进入现行架构。
- 用户明确停用一条路径时，应同时移除运行入口、能力声明和文档引用；历史数据及实验材料迁入带清单的存档，而不是继续留在正式目录制造误解。
- 来源载体、传输通道、执行引擎和供应商必须分开描述，避免把公众号、RSS、API、RedFox 等不同维度混为一类。

## 2026-08-27：先验证采集核心，不提前投入前端

- 当目标是搭建资讯采集链路时，首轮应优先完成真实数据获取、解析、存储和去重。
- “页面展示”只能作为后续验收界面，不能早于首个真实采集渠道占用实施时间。
- 在用户没有明确要求产品界面前，只保留最小 API/命令行验收方式。

## 2026-08-27：多来源先试爬，再冻结统一契约

- 新增站点时先按规范化域名去重，并逐站记录可访问性、robots、页面类型、内容边界和解析结果。
- 宽泛商业媒体可以用栏目和文章 URL 规则限定采集入口，但不能在采集阶段用行业关键词丢弃文章。
- 中英文来源使用同一内容契约，通过 `language`、`source_region` 和来源标签区分，避免维护两套加工链路。
- 遇到站点适配或反爬问题时，先查阅项目的参考实现清单，再决定使用静态 HTML、接口、RSS 或浏览器抓取。

## 2026-08-27：文章 tag 只采集，不在抓取时过滤

- 页面、Feed、JSON 上的 tag 属于文章元数据，应写入 `topics`，作为中游行业相关性的信号。
- 没有 tag、或 tag 不是美妆/护肤，都不能作为采集层丢弃理由；综合媒体仍要完整入库。
- 来源级 `source_tags` 描述栏目定位，不能代替单篇文章 tag。

## 2026-08-27：采集层按内容形式，中游按业务语义

- 采集层只判断页面是否为文章、新闻或报告，负责有效性、正文完整性、规范化和幂等入库。
- 美妆、品牌、融资、渠道等行业相关性属于中游内容加工和内容筛选，必须在原始内容保存之后执行。
- 业务筛选结果单独版本化，允许修改词典、模型或规则后对全量内容重跑，不能反向影响原始采集记录。
- 来源栏目和文章 URL pattern 是结构性发现规则，可以留在采集层；正文关键词和行业实体规则不能留在采集器里。

## 2026-08-27：来源身份与访问状态必须分开

- 来源目录应使用稳定的 `catalog_id` 关联数据库记录，入口 URL 改动时更新同一来源，不能因为 URL 变化生成重复来源。
- “当前抓取失败”不等于“站点永久不能爬”：维护页、Cloudflare 挑战、robots 禁止、站点关闭和解析规则错误必须分别记录。
- 只有经过多日定时运行才能称为稳定；单次成功统一表述为“当前已打通”。
- JSON 元数据字段必须显式配置；可选字段未配置时应写入 `null`，不能把整个接口对象误当成字段值。

## 2026-08-27：付费采集先消费本地归档

- 第三方接口按次计费时，已有 JSONL/快照必须优先走离线校验、幂等入库和重放，不为验证代码重复请求正文。
- 离线导入命令默认 dry-run，写库必须显式确认；在线来源默认停用，并同时由目录、调度和手动入口形成防误触边界。
- `meta_only` 和空正文只保留索引，不循环重打；需要再次联网前先给出预计调用次数和积分预算。

## 2026-08-27：契约升级不能改变历史内容身份

- 新增 external ID 时先匹配已有 external ID，再匹配旧 identity key，最后用规范化 canonical URL 兼容早期未规范化长链。
- 找到历史内容后永久沿用原 identity key；不能因为 URL 编码、query 顺序或 fragment 清理而生成第二条内容投影。
- 采集运行和快照属于溯源信封，不参与文章语义 payload hash，否则每次调度都会制造伪内容版本。

## 2026-08-27：领域能力必须配置化，不能把美妆写进平台内核

- Navigate 的目标是可扩展到全行业的通用资讯平台；美妆只是首个领域配置，不能成为事件、实体、评分、搜索或摘要模型中的固定前提。
- 文章契约、事件关系、实体主表、加工任务和搜索索引保持领域无关；领域分类、词典、事件类型、评分权重和 Prompt 通过可版本化的领域配置或数据库记录注入。
- 同一篇文章允许同时属于多个领域并分别保存置信度，不能再用单个 `is_relevant` 布尔值表达平台级归属。
- 通用算法信号与领域增强信号分开保存；没有加载任何领域包时，聚类、搜索和基础排序仍应正常工作。
- 现有 `industry-rules.v1` 只视为首个 `beauty` 领域处理器的历史实现，后续兼容迁移而不是继续扩散其命名和假设。

## 2026-08-27：面向中文读者的日报必须经过中文编辑加工

- 原始英文标题和 excerpt 只能作为证据输入，不能直接成为中文受众日报的最终成稿；需要可靠翻译、压缩和中文编辑表达。
- LLM 输出必须按内容或事件输入哈希缓存，保存模型、Prompt 版本和 Token 用量；密钥只从环境变量或安全交互读取，绝不进入仓库、数据库、日志或命令参数。
- 日报前台以新闻内容、来源和原文链接为主，不展示“未调用 AI”“算法版本”“媒体缺失”等开发说明；必要的数据口径压缩到页脚。
- 中文改写不得补充输入证据中没有的数字、人物、因果或结论；专有名词无公认译名时保留原文，来源摘要不足时明确收缩表达。

## 2026-08-28：日报日期、栏目与排序必须有独立业务契约

- 正式日报默认覆盖北京时间前一自然日，严格按来源 `published_at` 的半开区间筛选；不能用“数据库最新有内容日期”静默回退。
- 领域准入、是否收录、事件折叠、栏目归类和栏目内排序是不同决策，必须分别建模，不能用正文长度代替重要性。
- 通用渲染器只消费版本化编排契约，不认识“今日重点”“快讯”或任何行业栏目；行业栏目和排序规则属于领域配置。
- 单篇中文加工与整期编排分开缓存；整期只读取已校验摘要、标签和引用，不再次吞入全部正文。

## 2026-08-28：编辑简报不应混入运维与仪表盘信息

- 读者版刊头只展示出版日和内容覆盖日；采集截止时间属于内部审计数据，不应出现在日报正文或页脚。
- 故事数、来源数、近 7 日统计等仪表盘卡片只有在支持明确阅读决策时才展示；栏目本身已表达信息结构时应删除。
- 单篇来源发布时间不默认展示时区技术标记；需要时间线产品时再由独立模板呈现。

## 2026-08-29：归档可访问不代表来源仍在发布

- 恢复失败来源前必须先检查根域公告、现行栏目或 Feed 的最新发布日期，不能只凭搜索索引中的历史文章判定网站活跃。
- 临时维护、历史归档、正式关闭和持续发布是四种不同状态；恢复生产调度必须有当前发布证据。

## 2026-08-29：无偏移发布时间必须绑定来源时区

- 同一站的 RSS 可能给 UTC，而详情页 JSON-LD 只给本地墙上时间；不能默认把无偏移详情时间当 UTC。
- 来源配置应声明有效 IANA 时区；解析器将无偏移时间本地化后转 UTC，并用 Feed/详情 fixture 验证绝对时刻一致。
- 修复当前投影时保留已有不可变 Raw 版本，用新运行写入更正证据，不覆盖历史溯源。

## 2026-08-30：信源范围冻结后应立即转移产品重心

- 用户明确不再处理剩余网站后，应从正式路线图移除新增信源和失败站修复，不能继续把渠道扩张列为默认下一步。
- 现有信源足以验证产品闭环时，开发优先级转向实体、价值评分、跨来源事件效果、搜索热点和前端。
- 渠道层进入维护模式不等于宣称全部来源稳定；继续保留运行证据、合规边界和已打通/观察中的准确状态。

## 2026-08-30：LLM 实体候选必须与实体主库隔离

- 原文子串和偏移完全正确不代表实体类型一定正确；模型会把活动、栏目或出版物勉强塞入现有类型。
- LLM 只写候选和提及，未知候选保持 `entity_id=null`；只有唯一已确认别名可以自动解析，不能让模型直接创建或合并实体。
- canary 应限制批量、输入字符和调用次数，记录真实 Token；网络连接失败与模型调用必须分别计数，不能把连接前失败误报成已消费积分。

## 2026-08-30：人工审查原因也必须受证据约束

- 实体确认理由只能描述原文证据和本次类型判断，不能擅自翻译、扩写或替换公司身份；审计文字本身也可能制造错误事实。
- 创建实体前同时核对 surface、实体类型和标题上下文；不确定的母子品牌、公司英文名和人物关系继续保持 pending。
- 发现审计原因写错时应立即更正当前记录并在 lessons 中固化约束，不能因为实体链接结果碰巧正确就忽略错误说明。

## 2026-08-31：底层事件能力不等于前台一级栏目

- 事件聚类是去重、热点识别、相关报道和事件级摘要的中游能力，但不能仅因底层存在该对象就在首页设置一级入口。
- 多来源事件数量不足、覆盖不稳定时，前台统一展示精选资讯；未来达到稳定密度后，以读者语言“热点”或“相关报道”重新引入。
- 删除事件展示时必须把对应成员恢复到普通资讯流，不能因为移除卡片而让内容从首页消失。

## 2026-08-31：第三方密钥必须有统一入口和可验证状态

- 同一项目的 LLM 与采集 Provider 密钥统一由根 `.env` 或部署环境变量注入，不能让每个脚本自行 `source`、交互粘贴或选择不同文件。
- `.env.example` 只保留空变量名；真实 `.env` 必须 Git 忽略、权限 600，运行时环境变量优先且绝不被本地文件覆盖。
- 状态检查只允许显示 configured/missing、文件位置和权限，不得打印长度、前后缀或任何可用于识别密钥的片段。
- “适配器已接入”“来源已启用”“密钥已配置”“链路已稳定”是四种不同状态，汇报时必须明确区分。

## 2026-08-31：固定产品偏好不要暴露成无价值控件

- 用户已经确定阅读密度时，前端应直接采用该默认值并删除切换控件，不能让读者重复做产品已能确定的选择。
- 删除状态控件要同时删除响应状态、条件 class 和旧默认样式，避免界面虽然不显示按钮但仍以错误模式启动。
- 来源清单必须区分正式配置、数据库投影和历史存档；人工维护只改正式配置，再通过同步命令更新数据库。

## 2026-08-31：公众号显示名称不能替代 Provider 账号标识

- RedFox 作品列表要求微信号、原始 ID 或 bizInfo 三选一；只有显示名称的账号必须标为 pending，不能生成看似完整但无法运行的来源配置。
- 大批公众号应使用独立、去重、版本化的账号清单，再由目录层编译共享的 Provider 配置；不能在 `sites.json` 复制几十份相同端点和请求头。
- 正式清单、编译后的来源投影和历史存档保持单向关系；存档文件不可为了追平现状而改写，数据库也不能作为人工名单编辑器。
- 账号解析接口按次计费时，默认复用历史 fakeid/alias；未获得明确预算确认前不得为补齐清单自动逐个搜索。

## 2026-08-31：LLM 候选不能被确定性基线覆盖

- 混合分类器的确定性投影只能处理真正无需 LLM 的结果；综合来源待审和垂直来源待审都必须跳过，不能先写 LLM 结果再被规则基线覆盖。
- 增量 LLM 批处理除了检查工件缓存，还要检查当前内容哈希的有效投影；新增文章不能改变旧文章分组并触发重复计费。
- 每个分阶段命令执行后都应先确认下一阶段候选数没有异常增长；发生超预算调用时必须记录真实 Token 和根因，不能只报告最终成功。

## 2026-08-31：整期 LLM 编排必须有无损契约和失败用量审计

- “每个故事恰好出现一次”不能只写在 Prompt 中；模型可能为了去重或栏目数量主动省略内容，渲染前必须由程序核对 missing、duplicate 和 unexpected 引用。
- 模型仍负责栏目与主要顺序；契约层只在遗漏时依据已校验标签补入匹配栏目，不新增事实、不重写导语，也不为同一结构错误无限重试。
- Token 审计必须覆盖成功、校验失败、修复请求和异常退出。只在 CLI 成功结尾打印 usage 会漏掉真实消费，无法向额度敏感用户提供完整账单。
- 同一事件的跨来源改写未达到自动阈值时，优先使用带证据和操作者的 `must_link` 约束；不要为合并一个明显重复事件整体降低自动阈值。

## 2026-08-31：后一次子集清单不能覆盖历史主清单

- 用户给出一批“更新名单”时，必须先确认它是替换全集还是补充批次；没有明确替换意图时，应与此前人工主名单按稳定账号身份合并，不能只保留最近一批名称。
- 名称不是账号身份。品牌改名、空格差异或“集团/情报”等后缀变化应以 Provider 原始 ID 去重，并用别名保留用户曾填写的每个名称。
- 历史账号元数据已有可靠 fakeid/微信号时优先离线复用；不得为了重新发现已有身份而再次调用按次计费搜索接口。
# 2026-09-01：关闭特定付费渠道不等于禁止全部模型加工

- 用户要求因额度关闭 RedFox 时，只应把 RedFox 视为硬禁用渠道；若用户随后明确允许 LLM，则其他渠道入池后的中文化、领域判断和内容加工仍可按需调用。
- 节省额度的正确做法是先入原始池、限定新增内容、复用缓存并记录用量，不应擅自把“省积分”扩大成禁止所有 LLM。
# 主题发现必须形成持久内容闭环（2026-09-02）

- 用户点击“发现来源/发现主题”期待的是可阅读的新内容，不是一次性候选链接列表。
- Search 结果必须在用户额度内继续 Scrape，经过统一 `Source → CrawlRun → PageSnapshot → RawItem → ContentItem` 落库，再生成 `TopicMatch` 并立即刷新主题 Feed。
- 已存在的 URL 必须直接复用共享内容池，已抓取页面不得重复消耗 credits；外部发现结果不能只保存在前端响应中。
- 发现后前端应自动进入对应主题内容流，使用与“探索情报”一致的资讯卡片，而不是只在侧栏显示“最近发现”。

# 内容质量门禁要区分“日报准入”和“主题可见性”（2026-09-02）

- 待补全文、缺发布日期的内容不能进入正式日报或合并精选，但用户主动打开具体主题时仍应看见它们，并以最小化的“待补全”分组呈现。
- 隐藏不等于补齐。质量门禁必须保留回退抓取路径、原因和快照，不能用前端过滤掩盖内容不足。
- JSON API 的摘要或日期不完整时应优先复用同一规范网页的合规解析管道；保持 API 身份、避免重复请求、保留原始响应和失败警告。

# 个性化日报必须从主题匹配而非固定领域派生（2026-09-02）

- 用户选择主题后，历史日报的内容集合、日期列表和中文编辑工件都必须以该主题当前的可审计匹配为准，不能悄悄回退到 beauty 等平台领域。
- 日期窗口以内容的 `published_at` 为唯一依据；API 的“近 30 天”必须使用 CrawlRun 冻结的覆盖日计算，不能在重跑时以系统当前日期漂移。
- 缺失中文主题工件时可以调用模型补齐并缓存；模型只翻译、摘要和标记证据，不能改变主题匹配、发布日期或原文事实。

# 采集窗口与补全必须在统一入口收口（2026-09-02）

- 首次采集和日常增量是来源生命周期规则，应由 `CrawlRun.coverage_date` 冻结并在统一采集层执行，不能只给 API 或日报单独加日期过滤。
- 首次默认回溯 7 天、后续默认只收取覆盖日 1 天；发布日期缺失是待补全状态，不是直接拒绝条件。
- 内容或日期不足时，在同一次采集运行中自动尝试规范网页解析，保存二次快照和失败原因；不得用重新抓整站或重复入库来替代单篇补全。

# 新采集策略上线必须隔离旧数据可见性（2026-09-02）

- 新增 7 天首次窗口、1 天增量窗口不会自动改写历史入库事实；读者产品必须用版本化标记区分“按新策略验证”的内容与遗留池内容。
- 旧记录可保留给审计、去重和离线重处理，但不能自动进入新策略的主题日报，否则会产生看似“首次采集却出现去年内容”的误导。
- 对外 HTML 路由必须有认证后的端到端测试；仅验证数据组装函数不能证明页面能打开。

# 第三方搜索参数不能承载完整用户意图（2026-09-02）

- LLM 应先产出平台受约束的检索计划，再由程序编译 Firecrawl 参数；不能让模型直接生成可执行 Provider JSON。
- Firecrawl `location` 影响搜索本地化，不等同于文章发生地域；`tbs` 只适用于 `web` 结果，不适用于 `news`、`images`，更不能替代页面发布日期解析。
- 搜索返回的 title/description 只能作为候选证据；只有 Scrape 后经过 `article.v1.1`、发布日期验证、主题相关性与中文编辑工件，才可进入内容池和日报。
