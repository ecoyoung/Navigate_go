
## 当前任务：主题日报纸面编排（2026-09-02）

- [x] 主题日报用头条事实做导语，按标签或要闻/续闻分栏。
- [x] 刊头改为主题名，强调色保持 `#722F37`。
- [x] 阅读面去掉额度、待补全；发现来源留在主题管理。

## 当前任务：纸面编辑室品牌落地（2026-09-02）

- [x] 核对品牌包并写入纸面设计系统（色、字、标、图标用法）。
- [x] 登录 / 引导 / 工作台 / 移动导航对齐日报纸面，而不是 SaaS 深色台。
- [x] 日报模板强调色与 masthead 使用官方酒红 N 标。
- [x] 类型检查与本地预览，列出仍需用户补的素材。

### Review：纸面编辑室品牌落地

- 工作台与日报统一为象牙纸 `#F7F2E8`、酒红 `#722F37`、宋体刊头；图形标使用几何 N，不再使用 Inter 字标。
- 登录页改为纸面 masthead；文章流增加篇序与虚线分隔；日报模板内联同一 N 标。
- 品牌源文件放在 `navigate-brand/`，运行时副本在 `frontend/public/brand/`。
- 仍缺：宋体 outlined 字标、纸纹、空状态插画、1200×630 OG 图；这些不影响先发布当前纸面系统。

- 已去掉前台流程说明和日报“本期整理 N 条”盘点导语；空导语不再渲染，刊头只保留出版日期。

## 当前任务：本机 Docker + Cloudflare Tunnel 发布（2026-09-02）

- [ ] 为后端、前端和 cloudflared 增加 Docker Compose 编排，复用现有 SQLite 数据。
- [ ] 以 Nginx 同源反代 API，避免跨域 Cookie 在公网域名下失效。
- [ ] 用环境变量接入 Tunnel token，不把密钥写入仓库。
- [ ] 启动容器并验证本机 8080 与 Cloudflare 公网域名可访问。

# 资讯平台实施计划

> 本文件是按时间追加的实施日志，不代表当前架构。当前能力与目录结构以 `README.md` 和 `docs/architecture/` 为准。

## 当前任务：时间硬门禁、日报 500 与主题删除（2026-09-02）

- [ ] 复现并定位旧日期内容进入首次 7 天主题池的入口，改为发布日期硬门禁。
- [ ] 复现主题日报 Internal Server Error，修复根因并覆盖空日报/有内容日报。
- [ ] 增加仅限主题所有者的删除 API，清理关联运行、匹配与日报状态。
- [ ] 在“我的主题”加入明确的删除操作和二次确认，删除后刷新当前视图。
- [ ] 补回归测试、构建前端、重启服务并以当前真实数据验收。

## 当前任务：账户名登录、独立管理员模块与 50 条首次发现（2026-09-02）

- [x] 以可恢复归档方式重置实际运行的 SQLite 数据库，并从空库验证首个账号成为管理员。
- [x] 登录与注册只保留“账户名 + 密码”；管理员创建普通账号也不再要求姓名或邮箱。
- [x] 将账号管理从弹窗迁入管理员专属的一级工作台标签。
- [x] 将主题首次发现上限提升至 50，核验缺发布日期内容仍自动进入多信号网页解析补全。
- [x] 运行后端测试、前端生产构建并重启本地服务验收。

### Review：账户名登录、独立管理员模块与 50 条首次发现

- 已将运行中的 `backend/data/navigate.db`、WAL 与 SHM 文件归档到 `archive/2026-09-02-sqlite-reset/`，随后执行至 `0022_interest_topics` 的完整迁移。新库 `users` 数量为 0，`/health/ready` 返回 ready；首个通过注册页提交“账户名 + 密码”的账号自动成为管理员。
- 认证与管理员创建 API 都接受 `account` 字段，同时兼容历史 `email` 字段作为接口过渡；数据库内部列名暂不改动，避免无收益的大迁移。显示名称默认等于账户名。
- 管理员可从左侧“账号管理”进入独立工作台，创建、停用和恢复账号；账户弹窗只保留个人资料、改密、退出和跳转。
- Firecrawl 搜索/抓取的单次上限已提高为 50；首次主题会以 50 发起发现，缺日期或正文不足的候选仍自动进入网页日期解析补全和冷却重试机制。
- Ruff、154 项后端测试、Vue 类型检查/生产构建均通过；登录页浏览器检查仅显示账户名和密码，控制台无警告或错误。

## 待实施：多用户首次订阅引导与可用主题日报（2026-09-02）

- [x] 将未登录入口改为独立登录页；注册保留为次级入口，不展示内容工作台。
- [x] 登录后若无主题，展示全屏“告诉我，你想持续关注什么”气泡输入器；创建主题后自动启动首次 7 天发现。
- [x] 首次发现期间展示明确的阶段型进度条；完成后自动进入主题内容流。`topic_runs` 保留最终审计计数，实时逐条轮询留作后续扩展。
- [x] 修复主题发现内容不能进入日报的资格断点：按首次/增量窗口写入审计标记并严格按发布日期生成主题日报。
- [x] 增加管理员账号管理：创建用户、分配临时密码、停用/恢复账号；普通用户不得读取其他用户账号或订阅数据。
- [x] 补多用户隔离、首次自动采集、任务进度、日报可用性测试，完成本机构建与服务验收。

### Review：多用户首次订阅引导与可用主题日报

- 未登录状态只呈现登录/注册页；登录后的无主题用户进入居中的自然语言气泡输入器。创建后自动调用有界发现，前端以“理解主题、搜索、解析、入库、完成”阶段显示进度，并在完成后进入主题流。
- 主题发现写入 `collection_window:v1`，已有已发布主题内容也已无网络回填该标记；日报历史重新按 `TopicMatch + published_at + 质量门禁` 可用。
- 管理员 API 为 `/api/v1/admin/users`：创建账号必定强制首登改密；可停用/恢复普通账号，禁止停用当前管理员。所有端点在服务端按角色校验。
- Ruff、后端全量测试和 Vue 生产构建通过；本轮未触发外部采集。

## 当前任务：主题发现缺日期的统一网页补全（2026-09-02）

- [x] 核查待补全内容是否已经触发网页解析，并定位缺日期的实际断点。
- [x] 将 Firecrawl 发现内容的缺日期/缺正文统一接入合规网页解析、快照和冷却机制。
- [x] 让已存在但缺日期的发现内容可在冷却到期后重试，而不是永久被当成“非元数据内容”复用。
- [x] 补测试、验证 9 条现有待补全记录的重试资格并重启服务。

### Review：主题发现缺日期的统一网页补全

- 已核实“中国消费品牌出海东南亚的开店”主题的 10 条结果中有 9 条 `published_at` 为空：7 条有 Firecrawl Markdown 正文、2 条是元数据兜底。此前它们没有进入通用 HTML 解析回退。
- 主题发现现在把缺发布日期或正文不足的 Firecrawl 结果自动交给合规网页解析器，保存 `article_enrichment_web` 快照；成功后与原始发现身份合并并按 `article.v1.1` 更新内容。
- 已存在的 `needs_enrichment` 内容不再被“非 metadata_only”误判为可复用；冷却到期后会先直连原网页补全，不再重复 Firecrawl Scrape 或消耗其额度。失败写入时间戳，24 小时冷却后再试。
- Ruff、154 项后端测试和 Vue 生产构建通过。本轮只做本地测试，未实际请求这 9 个外站；因此它们会在下次点击该主题的“发现来源”时开始按新路径补全。

## 当前任务：多信号发布日期解析与存量刷新（2026-09-02）

- [x] 将发布日期解析扩展为 JSON-LD/OG、常见 Meta、Microdata、`time`、数据属性、URL 和可见日期的有序多信号策略。
- [x] 当网页正文不足时，仍单独解析页面级发布日期；日期成功后只补日期，不伪造正文。
- [x] 不调用 Firecrawl Scrape，直接刷新用户指定主题的 9 条缺日期内容。
- [x] 对不可访问或没有可靠证据的页面保留缺日期状态，不用转载文章或猜测日期覆盖。

### Review：多信号发布日期解析与存量刷新

- 每条成功解析的内容在质量警告中记录 `published_at:<origin>`，可区分 `json_ld`、`open_graph`、`meta`、`microdata`、`time`、`data_attribute`、`url` 和 `visible_text`；缺失则保留 `missing_published_at`。
- 对 9 条目标记录完成两轮原网页刷新，补齐 6 条；该主题当前 10 条收录内容为 7 条有日期、3 条无日期。三条无法补齐分别是原站 521、原站 403、以及页面没有可靠发布日期。
- URL 日期可识别紧跟文章 ID 的 `YYYYMMDD`，但仅在 1990 年至当前年份加一年内接受，避免把任意数字当作日期。
- 已运行 Ruff、后端全量测试和 Vue 生产构建；本轮没有调用 Firecrawl Scrape。

## 待实施：主题检索计划与 Firecrawl 参数编译（2026-09-02）

- [x] 定义版本化 `topic_search_plan.v1`：原始意图、纳入/排除、语言、内容地域、搜索位置、允许/排除域、时间窗与预算。
- [x] LLM 仅把自然语言编译为该受约束计划；程序再生成 Firecrawl `/v2/search` 请求，禁止让模型直接拼任意请求 JSON。
- [x] `web` 使用 `tbs` 做候选召回时间窗，`news` 不依赖该参数；全部结果 Scrape 后以页面 `published_at` 作为最终日期准入。
- [x] 把 Search/Scrape 响应转换为 `article.v1.1` 后，再由主题中文编辑工件生成标题、摘要、标签与日报内容。

### Review：主题检索计划与 Firecrawl 参数编译

- 新增 `topic-search-plan.v1`，并对主题 ID、意图哈希、查询、语言、内容地域、位置、域名和类别做严格校验；模型无法传入未知 Firecrawl 参数或同时配置允许/排除域。
- `/discover` 在调用 Search 前编译并缓存计划，审计运行记录保存非敏感计划和实际白名单请求；模型 token 用量写入 `topic_runs.llm_tokens_used`。
- 首次发现固定 `sources=[web] + tbs=qdr:w`，有成功发现记录后固定为 `sources=[web] + tbs=qdr:d`。这只是候选召回范围，正文 Scrape 与 `article.v1.1.published_at` 仍负责最终日期、质量和日报准入。
- Search 缓存指纹已覆盖 query、limit 和所有受支持参数，避免不同地域/域名/时间窗错误共享响应。
- Ruff 与 153 项后端测试通过；未为本轮验证调用 Firecrawl 或 DeepSeek。

## 当前任务：API 网页回退补全与历史日报（2026-09-02）

- [x] API 内容缺正文或发布日期时自动使用合规网页抓取管道补全，并保持同一内容身份。
- [x] 保存 API 与网页回退快照，失败不覆盖 API 原始记录，不重复请求已完整 URL。
- [x] 增加日报历史可用日期、按覆盖日读取和前端选择入口；日报继续严格按发布日期筛选。
- [x] 完成 API 回退、幂等、日报历史与前端构建回归验证。

### Review：API 网页回退补全与历史日报

- JSON API 详情正文不足 400 字、缺发布日期或未声明完整时，会保留 API 的外部 ID 与规范 URL，并自动抓取同源规范网页；网页快照单独保存为 `article_fallback_web`，合并只补强正文、日期、作者、媒体与标签。
- 已有 `verified_full` 内容不会被较薄 API 响应降级；规范 URL 等于 API 请求 URL 时不会重复请求。回退失败、robots 禁止和同 URL 情况均只写质量警告，API 原始入库仍继续。
- `GET /api/v1/daily-reports` 返回可用覆盖日及内容数，`GET /api/v1/daily-reports/{YYYY-MM-DD}` 直接渲染对应历史 HTML；两个接口只按 `published_at` 和读者质量门禁筛选。
- Vue 工作台侧栏提供历史日报日期选择和打开入口。单一主题流将已核验内容与“待补全”分组显示；合并流和日报仍保持严格质量门禁。
- 148 项后端测试、Ruff 与 Vue TypeScript/生产构建通过；本轮未调用 Firecrawl、RedFox 或 LLM。

## 当前任务：主题历史日报与 API 近 30 天窗口（2026-09-02）

- [x] 历史日报改为按用户主题、文章发布日期聚合，不复用固定 beauty 领域集合。
- [x] 复用或补齐主题中文编辑工件，确保主题日报标题、摘要与标签以中文输出。
- [x] API 采集默认限制为冻结覆盖日向前 30 个自然日，保留缺发布日期内容等待网页回退补全。
- [x] 调整 Vue 日期入口、补充测试并验证真实应用。

### Review：主题历史日报与 API 近 30 天窗口

- 主题日报由 `TopicMatch → ContentItem.published_at` 形成独立日期历史；不读取 beauty 领域准入或固定行业栏目。选择主题后才显示其历史日报入口。
- 日报按主题专属中文工件渲染，缺失的单篇工件以最多 12 篇一批调用 DeepSeek Flash 并缓存。已为“宠物电子产品出海”的一篇历史内容生成中文工件。
- API 默认窗口为冻结 `coverage_date` 向前 30 个自然日；未知日期不丢弃，继续等待网页回退补齐。窗口配置可由 `api_publication_window_days=0` 显式关闭。
- 真实内容池语言分布为中文 154、英文 37；英文来源有 18 个，但当前成功入池密度受其可访问性、robots 与解析状态影响。
- 150 项后端测试、Ruff 与 Vue 生产构建通过；后端已重启，主题日报路由已在 OpenAPI 中加载。

## 当前任务：统一首次/增量采集窗口与自动补全（2026-09-02）

- [x] 所有来源首次入库只接受覆盖日向前 7 个自然日的已发布日期内容。
- [x] 已有内容的来源每次更新只接受冻结覆盖日当天发布的内容。
- [x] 日期或正文不足的文章、报告自动走一次规范网页补全路径，保留快照、质量警告和原始记录。
- [x] 增加窗口边界测试并完成全量回归与服务重启。

### Review：统一首次/增量采集窗口与自动补全

- 窗口位于统一 HTTP 采集层，适用于网站、RSS、Sitemap 与 JSON API；业务领域不参与采集阶段判断。
- 首次窗口与增量窗口均基于 `CrawlRun.coverage_date` 冻结，默认分别为 7 天和 1 天；缺发布日期先允许入库/补全，避免把可修复候选静默丢弃。
- 提取结果日期缺失、正文不足 400 字或未标记完整时，同一运行自动尝试规范网页解析；成功后合并正文、日期、作者、媒体和标签，失败保留 `PageSnapshot` 与质量警告。
- 151 项后端测试、Ruff 与 Vue 生产构建通过；后端已重启。

## 当前任务：历史窗口基线与主题日报错误修复（2026-09-02）

- [x] 排除未经过新采集窗口验证的旧库内容，避免去年的遗留文章出现在新主题日报。
- [x] 为窗口内新入库内容写入可审计标记，主题日报只读取该标记与发布日期。
- [x] 新增认证后的主题日报 HTTP 渲染测试，覆盖中文 HTML 返回而非仅内部数据函数。
- [x] 完成全量回归并重启本机后端。

### Review：历史窗口基线与主题日报错误修复

- 去年内容来自新窗口规则上线前的历史入库，原先动态历史日报没有区分采集政策版本。现有旧记录保留在内容池作审计和再处理，但不再进入主题日报。
- 新入库的窗口内内容写入 `collection_window:v1` 质量标记；主题日报要求该标记、有效 `TopicMatch`、可读质量和 `published_at` 四项同时成立。
- 主题日报端点已用真实登录会话测试：返回 200 与中文 HTML，覆盖此前漏掉的路由/渲染组合。
- 152 项后端测试、Ruff 与 Vue 生产构建通过；后端已重启。

## 当前任务：内容质量门禁与去重补抓（2026-09-02）

- [x] 定义统一内容质量等级：完整可用、部分可用、待补抓。
- [x] 日报、主题流和内容 API 统一使用质量等级，不把元数据兜底当成完整原文。
- [x] 对 API 无发布日期和未知正文完整度显式标记，保留入池但禁止进入日报。
- [x] 对主题发现加入共享 URL 复用和失败补抓冷却，避免重复抓取与重复计费。
- [x] 完成回归测试、数据库检查、前端构建、重启与 Review。

### Review：内容质量门禁与去重补抓

- 新增 `verified_full / partial / needs_enrichment` 三档；日报和默认主题流只使用前两档，内容 API 返回当前档位供后台核验。
- API 新入库仍使用 `article.v1.1`；无发布日期自动加入质量警告，现有库无网络回填 54 条警告，未删除原始内容或抓取记录。
- 完整内容以共享规范 URL 复用；元数据抓取失败记录写入内容质量并以 24 小时冷却限制跨主题重复 Scrape。
- 147 项后端测试、Ruff、Vue 生产构建及 SQLite 完整性检查通过；后端已重启。

## 当前任务：个性化主题 LLM 智能加工层（2026-09-02）

- [x] 审计并复用现有 DeepSeek Flash 客户端、缓存指纹和中文编辑工件。
- [x] 定义 `topic_intent_llm.v1`：实体、行业、产品、事件、地区、正向及排除条件。
- [x] 定义 `topic_content_editorial.v1`：中文标题、摘要、标签、相关性、事件类型及排序信号。
- [x] 实现按主题哈希和内容哈希缓存的批量加工，明显规则结果不重复调用 LLM。
- [x] 将验证后的输出写入通用 LLM 工件，并更新 TopicMatch 排序与展示。
- [x] 对“中国宠物电子产品出海”现有 10 条内容执行真实加工并记录 tokens。
- [x] 完成测试、构建、数据库完整性、重启与 Review。

验收标准：中文主题流不再直接展示未经加工的英文标题；主题意图具备可审计结构；每篇内容有中文标题、摘要和标签；不相关内容退出 include；相同主题/内容重跑命中缓存；LLM 用量可准确报告。

### Review：个性化主题 LLM 智能加工层

- 新增两份严格契约和主题专属工件，单主题最多 12 篇一次批量比较；缓存包含输入、Prompt、Schema、校验器和模型指纹。
- 正式主题 10 条处理后收录 8 条、排除 2 条；成功文章批次 15,641 tokens，随后复跑两层缓存均命中且新增用量为 0。
- Provider 字段同义词只做白名单改名；主题、内容哈希、输入顺序、中文字段和逐字证据仍严格校验。早先被拒绝的修复调用未保存 usage，因此完整账单只能报告下限，已列为下一优先事项。
- 主题页读取主题专属中文标题、摘要和标签，按相关分优先排序；合并流仍按发布时间优先，同一内容去重。
- Ruff、144 项后端测试、Vue 类型检查/生产构建通过；SQLite `integrity_check=ok`、外键违规 0。

## 当前任务：打通主题发现到持久内容池（2026-09-02）

- [x] 确认断点：现有 Firecrawl Search 仅保存候选来源，没有 Scrape、内容入库和 Feed 刷新。
- [x] 在主题每日额度内抓取候选页面，优先复用已存在 URL。
- [x] 将新页面写入统一 Source、CrawlRun、PageSnapshot、RawItem 与 ContentItem。
- [x] 为发现内容建立可重算的主题匹配，并在 API 响应中返回持久 Feed。
- [x] 前端发现成功后自动进入该主题，以探索情报相同的卡片展示内容。
- [x] 为用户刚创建的主题补跑已有 10 个候选，并核验重启后仍可读取。
- [x] 完成测试、构建、数据库完整性、浏览器与 Review。

验收标准：发现的文章不是临时响应；刷新页面和重启服务后仍存在；同一 URL 不重复入库或重复计费；总调用不超过用户主题当日额度；排除关键词仍优先；前端不只展示候选来源。

### Review：打通主题发现到持久内容池

- `/topics/{id}/discover` 现在完成 Search、候选保存、共享 URL 复用、有界 Scrape、统一内容入库、主题匹配和 Feed 返回；响应新增 `fetched_pages`、`ingested_count`、`metadata_only_count` 与持久 `items`。
- 完整页面严格写入默认停用的来源证据、CrawlRun、PageSnapshot、RawItem 和 `article.v1.1 / ContentItem`；额度不足或慢页使用 Search 标题、摘要和原文链接建立 `metadata_only` 内容，不丢失已发现结果。
- 每篇外部请求前不持有 SQLite 写事务，成功后逐篇提交；相同规范 URL 复用现有 ContentItem，不再次 Scrape。明确排除词仍可拒绝，跨语言 Search 命中不再因为缺少完全一致中文词而自动排除。
- 前端点击“发现来源”后自动进入该主题，使用与探索情报相同的 `intel-card` 资讯流；候选侧栏保留为辅助信息，刷新时主题 Feed 从数据库重读。
- 用户的“中国宠物电子产品出海”主题现有 10 个候选全部持久化并建立 include 匹配：1 篇完整正文、9 篇 metadata-only，10 个候选均关联来源证据。
- 首次旧批量补跑在联网期间持有事务且没有提交运行记录；为避免低估额度，已按其最多 10 次 Scrape 记入保守上界，加原 Search 2 和后续成功 Scrape 1，当日计费保护按 13/15 credits 执行，实际 Provider 用量可能更低。
- Ruff、141 项后端测试、Vue 类型检查/生产构建通过；SQLite `integrity_check=ok`、外键违规 0；后端已用修复代码重启。metadata-only 文章在以后再次发现且额度允许时会原位升级为完整正文，不会形成重复内容。

## 当前任务：Firecrawl 接入设计与个性化主题订阅改版（2026-09-02）

- [x] 将 Firecrawl 密钥纳入根 `.env`、示例槽位和统一脱敏状态检查。
- [x] 阅读 Firecrawl 官方 API、功能、计费/限制和自托管边界。
- [x] 只读拆解 `aihot.virxact.com` 的信息架构、视觉系统和核心交互。
- [x] 将固定领域订阅重构为关键词/自然语言主题订阅数据契约与处理链设计。
- [x] 明确 Firecrawl 在“来源发现、抓取、定时监控”中的职责和成本保护规则。
- [x] 输出网站信息架构、关键页面、设计系统与 v0 范围，等待用户确认。
- [x] 用户确认设计方向后实现数据库迁移、API 和 Vue v0。

### v0 执行清单

- [x] 新增兴趣主题、来源候选、主题匹配和运行审计表。
- [x] 实现主题创建、修改、暂停、现有池预览和个人合并资讯流 API。
- [x] 实现 Firecrawl Search/Scrape 客户端、请求缓存、每日额度与页数硬限制。
- [x] 将 Vue 首页升级为“为你精选 / 我的主题 / 探索”工作台。
- [x] 完成一次有界 Firecrawl canary，并记录实际 credits/结果。
- [x] 完成迁移、测试、构建、浏览器验收与 Review。

验收标准：Firecrawl 密钥不出现在日志、状态输出或仓库跟踪文件中；用户可定义任意主题意图而非只能选 beauty；采集候选必须进入统一数据池后再匹配主题；设计明确额度上限、去重共享和失败降级；按网页设计流程在写完整界面前等待设计系统确认。

### Review：个性化主题订阅与 Firecrawl v0

- Alembic `0022` 新增 `interest_topics`、`topic_source_candidates`、`topic_matches`、`topic_runs` 和 `provider_request_cache`；正式 SQLite 已升级到 head，`integrity_check=ok`、外键违规 0。
- 新增主题创建、列表、修改、预览、单主题 Feed 和个人合并 Feed API；创建/修改从共享内容池执行版本化确定性匹配，同一内容命中多个主题时只展示一次。
- Firecrawl v2 Search/Scrape 从统一 `FIRECRAWL_API_KEY` 读取，Search 单次最多 10 条，主题日额度 API 上限 20，12 小时全局缓存；Search 结果只成为候选来源，不自动注册、不执行无限 Crawl。
- 真实 canary 的 Search 返回 3 条 `docs.firecrawl.dev` 结果，Scrape 1 个官方页面成功，本次实际最大消耗 3 credits；输出未包含 key 或正文。
- Vue 首页替换为深色个人情报工作台，包含为你精选、我的主题、探索情报、主题创建/暂停/来源发现、额度和候选来源视图；移动端页面与登录弹窗已完成浏览器验收。
- Ruff（维护代码和新迁移）、140 项后端测试、Vue 类型检查和生产构建通过；后端已重启在 `8000`，前端本地生产预览继续运行在 `3000`。
- v0 主题意图编译和匹配仍为确定性规则；LLM 结构化编译、边界复核、候选来源审核注册与个人日报投递留到下一阶段。

## 当前任务：停用 RedFox、直连渠道批采与 SQLite 账号订阅（2026-09-01）

- [x] 在批采命令层显式排除 RedFox，并核验正式 RedFox 来源全部停用。
- [x] 批量抓取网站、RSS、Atom 与普通 API 渠道，将有效内容落入现有数据池并记录增量。
- [x] 定义通用的用户、会话、领域订阅数据契约并新增 SQLite 迁移。
- [x] 实现注册、登录、退出、改密、当前账号与订阅 API。
- [x] 通过本地 CLI 创建首个管理员账号，明文临时密码不写入数据库或仓库。
- [x] 在 Vue 前端接入账号入口、登录/注册和领域订阅状态。
- [x] 迁移正式数据库，运行后端测试、前端类型检查/构建、数据库完整性与本地联调。
- [x] 更新 README、进度说明与本任务 Review。

验收标准：本轮 RedFox 调用为 0；LLM 仅在其他渠道新增内容确需加工时调用、优先复用缓存并记录用量；批采命令无法误触 RedFox；密码使用带盐强哈希、会话只存令牌哈希；首个管理员存在且可登录；账号可订阅/暂停任意领域，不把美妆写死在通用数据模型中；其他渠道的真实采集结果可追溯地进入现有内容池。

### Review：停用 RedFox、直连渠道批采与 SQLite 账号订阅

- `crawl_catalog` 新增可重复的 `--exclude-provider`，在来源调度和停用判断前直接移除目标 Provider；测试证明 RedFox 项不会进入运行结果。本轮正式库 244 个 RedFox 来源启用数仍为 0，真实报告含 0 个 RedFox 项和 0 次 RedFox 调用。
- 联网批采 33 个非 RedFox 来源：23 成功、5 失败、3 阻断、2 不可用，内容池由 148 增至 161，新增 13 条。5 个失败分别为 Beauty Independent 403、Beauty Packaging/HAPPI/Cosmetics & Toiletries robots 403、魔镜接口 500，均按真实失败保留。
- 新增内容完成确定性加工；beauty 准入仅对 3 条边界内容调用 1 个 DeepSeek 批次，prompt 3,086、completion 422、合计 3,508 tokens，3 条均收录，活动分类器继续为 `domain_hybrid / beauty-domain-hybrid.v1`。
- 新增 `users`、`auth_sessions`、`user_subscriptions` 和 Alembic 0021：密码用 scrypt 随机盐，Cookie 为 HttpOnly/SameSite=Lax，会话库只存 SHA-256 token hash；订阅唯一键为用户、领域、投递类型，不绑定美妆。
- 注册、登录、退出、当前账号、改密、订阅查询和启用/暂停 API 已完成；首管理员只能由本机 CLI 创建，普通注册在管理员存在前返回 503。正式库已创建 1 个管理员且要求首次改密。
- Vue 首页新增账号入口、登录/注册弹窗、首次改密提示和每日简报订阅按钮；浏览器实测入口及弹窗可见。本地前端 `3000`、后端 `8000` 均运行，CORS 返回精确来源且允许凭据。
- Ruff、136 项后端测试、Alembic head、Vue 类型检查/生产构建通过；SQLite `integrity_check=ok`、外键违规 0。账号订阅只记录状态，邮件/消息投递尚未实现。

## 当前任务：补齐历史公众号主清单（2026-08-31）

- [x] 对照当前 51 项正式清单与此前人工填写、历史账号元数据，找出全部漏项。
- [x] 确保包含卡诗Kerastase、潘婷、尼尔森IQ、凯度KANTAR。
- [x] 为新增账号复用已有可靠标识；只有确实缺失时才调用 RedFox 搜索。
- [x] 同步正式数据库，所有公众号继续保持自动调度停用。
- [x] 完成数量、唯一性、目录编译、数据库和回归测试验证。
- [x] 更新 README、进度与 Review。

验收标准：正式清单覆盖此前用户填写的完整范围，图片指定的 4 个公众号全部存在；显示名称不冒充 Provider 选择器；新增项可编译但默认停用；不重复付费搜索已有账号元数据。

### Review：补齐历史公众号主清单

- 找到此前人工填写的 206 项主名单，与当前 51 项按 RedFox 原始账号 ID 合并；13 个身份重合，其中 3 个属于改名，最终形成 244 个唯一账号、247 个名称。
- `aliases` 正式承载“自然堂/自然堂集团”“花西子 Florasis/花西子Florasis”“青眼情报/青眼”，避免同一账号重复采集和重复计费。
- 卡诗Kerastase、潘婷、尼尔森IQ、凯度KANTAR 均使用既有 fakeid/微信号证据，状态为 ready、enabled=false；本任务没有调用 RedFox 或 LLM，新增积分消耗为 0。
- 数据库同步复用 63 个历史来源及既有内容关系，新建 130 个缺失来源；最终共 277 个来源，其中正式 RedFox 244、启用 0、历史平行 `redfox_archive` 0。
- Ruff、131 项后端测试通过；目录 277 项唯一，SQLite `integrity_check=ok`、外键违规 0。

## 当前任务：2026-08-30 全渠道真实情报（2026-08-31）

- [x] 使用 RedFox 搜索补齐 38 个 pending 公众号，并复用缓存完成歧义确认。
- [x] 同步 51 个公众号来源，全部保持自动调度停用。
- [x] 修复 Provider robots 检查目标并以 36氪完成目标日 canary。
- [x] 执行 51 个公众号的 2026-08-30 一次性采集。
- [x] 执行当前网站、RSS、普通 API 全渠道采集。
- [x] 重跑内容加工、领域准入、事件和精选评分。
- [x] 调用 LLM 补齐中文工件并生成 2026-08-30 日报。
- [x] 重建本地前端，验证日报日期、内容与原文入口。
- [x] 记录 RedFox/LLM 用量、失败来源和 Review。

验收标准：日报覆盖日严格为 2026-08-30；只使用真实采集与可追溯原文；公众号与网站等可用渠道共同参与；RedFox 与 DeepSeek 调用有缓存和实际用量记录；余额不足等外部失败不能伪报为成功。

### Review：2026-08-30 全渠道真实情报

- 38 个 pending 公众号通过 RedFox 官方账号搜索全部解析完成；正式清单现为 51 ready、0 pending、0 enabled，数据库为 51 个正式 RedFox 来源、0 enabled，另保留 63 个停用历史来源。
- RedFox 目标日采集含 36氪 canary：合计 38 个来源完整成功、1 个部分成功、12 个失败；新增 24 条 2026-08-30 公众号内容。失败根因是 Provider 余额耗尽至 0，未伪报成功且未继续重试。
- 网站/RSS/API 同步执行，批次 24 成功、4 失败、3 阻断、2 不可用，新增 14 条；目标日仅 1 条网站内容且未通过 beauty 领域准入，因此最终日报内容来自公众号，但全渠道均真实参与。
- 中游重跑后数据库为 148 条内容、146 个活动事件、2 个多篇事件；评分运行 8 输入 72、精选 24。天玺国际奠基的两篇跨来源报道通过审计约束归并，日报由 11 篇相关文章形成 10 个故事。
- 最终文件为 `output/daily/2026-08-30-beauty.html`，并同步到 Vue 本地站点；浏览器验证覆盖日、10 个故事、原文链接、无 CST、无 AI/算法说明，首页日报入口动态指向该文件。
- 本轮可从成功命令确认的 DeepSeek 用量下限为 73,792 tokens；旧流程未持久化逐篇日报工件和失败校验重试的 usage，完整账单不可准确还原。后续需先补 usage 审计再进行大批量付费运行。
- Ruff、131 项后端测试、Vue 类型检查与生产构建通过；SQLite `integrity_check=ok`、外键违规 0。

## 当前任务：更新 51 个公众号正式清单（2026-08-31）

- [x] 将用户给出的名称去重并与 206 条历史账号元数据匹配。
- [x] 新建独立、可手工维护的 `redfox-wechat-accounts.v1` 清单。
- [x] 从 `sites.json` 移除重复的单个公众号配置，由目录加载器统一编译。
- [x] 可靠匹配项标记 ready，缺少微信号/bizInfo 的项目标记 pending。
- [x] 离线副本预演后同步正式数据库，全部保持停用。
- [x] 完成数量、唯一性、配置、数据库与全量测试验证。
- [x] 更新架构、维护说明与 Review。

验收标准：正式公众号清单恰好 51 个唯一名称；显示名称不得冒充 RedFox 账号参数；pending 项不能进入运行目录或被启用；同步不联网、不调用付费接口、不破坏历史内容关系。

### Review：更新 51 个公众号正式清单

- 新增 `backend/config/wechat_accounts.json` 作为唯一正式公众号名单，Schema 为 `redfox-wechat-accounts.v1`；51 个名称全部唯一且默认停用。
- 基于历史 fakeid/alias 证据解析 13 条为 ready，其余 38 条为 pending；没有把公众号显示名称伪装成微信号，也没有调用按次计费的 RedFox 搜索接口。
- `load_catalog()` 将 ready 账号编译成完整 `third_party_feed / provider_api / redfox` 来源；pending 账号只保留在名单，不能进入调度。
- `sites.json` 中原“化妆品报”重复对象已移除；历史存档的 names/accounts 文件保持不可变，不再作为运行时输入。
- 正式数据库同步为 RedFox 13 条、启用 0；另有 63 条停用 `redfox_archive` 仅保留历史内容关系。同步前备份为 `/tmp/navigate_before_wechat_51.db`。
- Ruff、125 项测试、JSON Schema 约束、数据库完整性和外键检查通过；本任务未调用 RedFox、DeepSeek 或其他付费 API，新增额度消耗为 0。

## 当前任务：公众号清单定位与固定紧凑阅读（2026-08-31）

- [x] 区分正式 RedFox 来源配置、数据库投影与历史公众号存档。
- [x] 移除前端阅读密度控件和响应状态。
- [x] 将资讯流固定为紧凑间距及单行摘要。
- [x] 补充公众号清单的人工维护与同步说明。
- [x] 重建本地版本并完成 Review。

验收标准：首页不显示“阅读密度”，初始和全程均使用紧凑布局；明确指出正式公众号配置的唯一维护入口，历史清单不被误认为运行时配置。

### Review：公众号清单定位与固定紧凑阅读

- 正式公众号配置只有 `backend/config/sites.json` 中的 `huazhuangpinbao_wechat`；修改后运行 `make seed-catalog`。数据库是配置投影，不作为人工编辑入口。
- 存档 `wechat_mp_accounts.json` 保存旧的大批名单；数据库对应 71 条 `redfox_archive` 历史来源，全部停用且不参与正式运行。
- 前端删除 `compact` 响应状态、阅读密度按钮及相关 CSS；资讯行固定为 `18px 0 20px` 间距，摘要固定一行。
- Vue 类型检查和生产构建通过；本地首页返回 200，客户端包不含“阅读密度/舒展/紧凑”控件文字，12 条资讯保持完整。
- 本任务未调用 LLM、RedFox 或任何付费接口，新增积分消耗为 0。

## 当前任务：统一 API 密钥管控（2026-08-31）

- [x] 审计 DeepSeek、RedFox 的密钥读取点与正式接入状态。
- [x] 将两个密钥变量统一到项目根 `.env`，并保持 600 权限及 Git 忽略。
- [x] 自动加载根 `.env`，外部运行环境变量优先且不被文件覆盖。
- [x] 移除 LLM 命令的交互式 key 回退，缺失时在联网前失败。
- [x] 增加不显示值的 `check-secrets` 状态检查与回归测试。
- [x] 验证 RedFox 来源、执行引擎、启用状态和项目内硬编码泄漏。
- [x] 更新 README、进度与 Review。

验收标准：DeepSeek 与 RedFox 只通过 `DEEPSEEK_API_KEY`、`REDFOX_API_KEY` 注入；两者变量位于同一根 `.env`；日志、错误、API、仓库文件与命令参数不出现密钥值；缺失密钥不能发起外部请求。

### Review：统一 API 密钥管控

- 根 `.env` 只声明 `DEEPSEEK_API_KEY` 和 `REDFOX_API_KEY`，权限为 600；`.env.example` 同时提供两个空槽位，真实值仍被 `.gitignore` 排除。
- 新增统一 secrets 模块，使用 python-dotenv 自动加载根文件且 `override=false`；部署环境注入值优先于本地文件。
- 四个 DeepSeek CLI 和 RedFox 请求头均改用 `require_secret`；缺失时只输出变量名，不回显值，也不再提示在终端临时粘贴。
- `make check-secrets` 只报告 configured/missing 与文件权限。当前 DeepSeek 与 RedFox 均为 configured。
- RedFox 已接入正式 `third_party_feed → provider_api → redfox` 执行链，但来源仍为 disabled；配置 key 不会自动启用或触发采集。
- 维护代码范围未发现 `sk-...` 硬编码；Ruff、124 项后端测试和 uv 锁文件更新通过。本任务未发起 DeepSeek 或 RedFox 网络请求，积分消耗为 0。

## 当前任务：移除前端跨来源事件模块（2026-08-31）

- [x] 明确只移除前台入口与展示，保留后端事件聚类能力。
- [x] 删除事件导航、事件视图和重点事件卡片。
- [x] 让被事件卡片占用的内容重新进入统一精选资讯流。
- [x] 清理无用 TypeScript 类型、状态和 CSS。
- [x] 重建并验证本地生产版本，更新 Review。

验收标准：首页不再出现“事件”“跨来源事件”“今日重点事件”模块；12 条精选均可在统一资讯流中浏览和搜索；后端事件数据与 `navigate-home.v1` 契约不受影响。

### Review：移除前端跨来源事件模块

- 删除顶部“事件”导航、跨来源事件视图和首页重点事件卡片；对应 TypeScript 状态、事件展示类型与 CSS 一并移除。
- 精选计算不再排除首个事件成员，`navigate-home.v1` 的 12 条故事全部进入统一资讯流；搜索和阅读密度切换保持不变。
- 后端 109 个活动事件及快照中的 1 个多来源事件仍保留，只是不在当前首页渲染，后续可用于热点或相关报道。
- Vue 类型检查与生产构建通过；本地 `http://127.0.0.1:3000/` 返回 200，快照 story_count=12，客户端包不包含“跨来源事件”或“今日重点事件”。
- 本任务未调用 LLM、RedFox 或任何付费接口，新增模型积分消耗为 0。

## 当前任务：中文前台与美妆准入纠偏（2026-08-31）

- [x] 记录前台说明、中文工件和领域误收录的纠偏规则。
- [x] 增加版本化美妆领域准入策略，区分垂直来源与综合来源逐篇判断。
- [x] 重跑领域归属和精选评分，排除 IBM 体育、肠类制品及垂直来源跨行业内容。
- [x] 补齐精选英文内容的缓存化中文标题、摘要和标签。
- [x] 升级首页数据契约，禁止英文内容回退到原文展示。
- [x] 删除前台价值分、算法说明、聚类说明和多源信号说明组件。
- [x] 重建本地生产版本并验证首页和数据入口。
- [x] 更新架构、进度与 Review。

验收标准：综合来源只在单篇具有 beauty 领域证据时进入；用户指出的两篇无关内容不再出现；英文内容只有具备合格中文编辑工件才能进入前台；界面以资讯内容为主，不展示内部评分与流程解释；LLM 调用复用缓存并报告实际用量。

### Review：中文前台与美妆准入纠偏

- 新增 `beauty.v1` 领域策略、确定性初筛和缓存化 LLM 逐篇复核；活动分类器显式固定为 `domain_hybrid / beauty-domain-hybrid.v1`，评分不再错误读取旧版本 include 记录。
- 110 条内容最终 beauty include 55、exclude 55；评分运行 6 输入 55、精选 12。IBM 体育、星图肠类制品、AI*bio 和垂直媒体中的非美妆可穿戴文章均退出前台。
- `navigate-home.v1` 只输出可展示的中文标题与摘要；12 条精选中非中文原文 9 条，缓存命中 9、缺失 0、英文回退 0。
- Vue 界面删除内部价值分、算法/聚类说明、阅读提示和多源信号卡片；生产构建已由 `http://127.0.0.1:3000/` 返回 200。
- 本次领域复核累计 10 个 DeepSeek 批次，prompt 66,429、completion 9,375、total 75,804 tokens；其中最后一轮垂直来源复核为 18,150 tokens。中文编辑的早期批次没有保存可分摊 usage，无法从数据库准确追溯，不将其记为 0。
- Ruff、121 项后端测试、数据库完整性/外键检查、Vue 类型检查和生产构建全部通过；快照中 4 个指定误收录 ID 和非中文展示项均为 0。

## 当前任务：切换为本地生产预览（2026-08-31）

- [x] 记录访问与发布目标纠偏经验。
- [x] 重新构建 Vue 前端生产版本。
- [x] 启动本机可访问的生产预览服务。
- [x] 验证首页、数据快照和日报入口。
- [x] 更新当前交付状态与 Review。

验收标准：用户可通过本机地址直接访问，不依赖 ChatGPT Site 权限；服务读取当前 Vue 构建和真实快照；不调用 LLM。

### Review：切换为本地生产预览

- 当前交付入口改为 `http://127.0.0.1:3000/`，直接托管 Vue 生产构建，不经过 ChatGPT Site 身份验证。
- 新增 `npm run local` 固化本地构建与服务入口；README 明确本地地址是当前交付基线。
- 首页、`navigate-home.v0` 数据快照和完整日报入口均返回 200；本地服务保持运行。
- 本任务未调用 LLM、RedFox 或任何付费接口，新增模型积分消耗为 0。

## 当前任务：前端迁移至 Vue 3 + TypeScript（2026-08-30）

- [x] 记录技术栈纠偏经验，并核对现有前端与托管约束。
- [x] 将 React/Next/Vinext 实现迁移为 Vue 3 Composition API + TypeScript。
- [x] 保留真实快照、日报入口、视觉系统和核心交互。
- [x] 完成类型检查、生产构建和本地入口验证。
- [x] 发布到既有站点并核验线上状态。
- [x] 更新项目文档与本任务 Review。

验收标准：`frontend` 的运行时和源码不再依赖 React/Next/Vinext；Vue TypeScript 类型检查与生产构建通过；精选/事件切换、搜索、密度切换、多来源展开及日报入口继续可用；只读取现有真实快照，不调用 LLM。

### Review：前端迁移至 Vue 3 + TypeScript

- 运行时改为 Vue 3.5.42，使用 `<script setup lang="ts">`、Composition API、严格 TypeScript 和 Vite；React、Next、Vinext 及相关类型和构建依赖全部移除。
- 首页继续读取 `navigate-home.v0`，保留精选/事件切换、关键词搜索、阅读密度、多来源报道展开、日报入口、加载/错误/空结果及响应式布局，没有改写后端数据。
- Cloudflare Worker 入口仅负责静态资产交付，生产构建继续输出 Sites 所需的 `dist/server/index.js`、客户端资产和托管元数据。
- `vue-tsc --noEmit`、生产构建和部署包校验通过；本地首页、`home.json`、日报入口均返回 200；生产依赖安全审计为 0 漏洞。
- 既有站点已发布版本 2 并处于 active；本任务没有调用 DeepSeek、RedFox 或任何付费 LLM，新增模型积分消耗为 0。

## 当前任务：跨来源事件校准与资讯前端 v0（2026-08-30）

- [x] 审计现有事件候选与真实跨来源相同事件，建立保守验收样本。
- [x] 调整通用聚类信号或人工约束，形成首批可解释跨来源事件。
- [x] 重建事件与价值评分，核验事件、精选池和幂等。
- [x] 研究公开资讯平台的现行信息架构与交互模式。
- [x] 盘点现有前端、API 和真实可展示数据，声明设计系统。
- [x] 交付可浏览的前端 v0，完善完整状态与响应式体验。
- [x] 运行前后端、站点和数据库验证，更新架构、进度与 Review。

验收标准：跨来源事件必须有真实标题/正文证据和独立来源，不因行业关键词相同而误合并；评分只读取活动事件；前端使用真实 API 数据、不伪造数字，包含日报/精选/事件的核心阅读路径；不调用 DeepSeek LLM。

### Review：跨来源事件校准与资讯前端 v0

- 审计 1887 对跨来源候选后，自动最高分仅 0.2101；直接降低 0.78 自动阈值会制造大量误合并，因此保持自动配置不变。
- 内容 39 与 93 的贝泰妮半年报核心证据一致，人工创建 constraint 1。事件运行 5 形成 109 个活动事件、1 个双来源事件；两个成员保存 manual、约束 ID、理由和操作者。
- 事件重建第二次复用 run 5；评分运行 2 纳入两来源佐证，贝泰妮两篇得分升至 71.23、69.46，精选仍为 21；重复评分复用 run 2。
- 新增正式数据快照导出器，输出 `navigate-home.v0`：21 篇精选、全量池/事件口径、日报 lead、已缓存中文标题摘要和多来源成员；不调用 LLM。
- 前端采用暖象牙纸、酒红和中文宋体编辑风格；实现精选/事件切换、搜索、阅读密度、来源展开、日报入口、加载/错误/空结果及移动端布局。
- 公开界面参考 Ground News 的事件主卡、Techmeme 的主报道+更多来源、Feedly 的高密度阅读队列；没有复制品牌资产或政治偏向评分。
- 后端 Ruff、118 项测试、数据库完整性和外键检查通过；前端 ESLint、生产构建、首页/数据快照/日报三个 HTTP 入口均通过。
- 本任务未访问 RedFox 或调用 DeepSeek，新增积分消耗为 0。

## 当前任务：独立资讯价值评分 v1（2026-08-30）

- [x] 定义领域无关的评分契约、信号、门槛和可解释输出。
- [x] 增加评分运行、逐篇结果模型、Alembic 迁移及版本化配置。
- [x] 实现确定性评分、全量池/精选池决策、输入指纹和幂等复用。
- [x] 增加评分 CLI、查询 API 和回归测试。
- [x] 用正式库预演分布并校准配置，不调用 LLM。
- [x] 应用正式评分并核验精选结果、数据库完整性和重复执行。
- [x] 更新架构、进度与本任务 Review。

验收标准：评分与领域归属、事件聚类及 LLM 输出独立存储；正文长度只作为完整性而非重要性；每个总分都能还原分项、惩罚和入选理由；未确认实体不参与；相同输入、配置和冻结时点重复执行不产生新结果；本任务不调用 LLM。

### Review：独立资讯价值评分 v1

- 新增 Alembic 0020、`content_value_score_runs` 与 `content_value_scores`；运行冻结领域、配置、`as_of` 和完整输入指纹，逐篇保存分项、扣分、门槛、理由与全量池/精选池决定。
- `content-value-score.v1` 将时效、来源类型、完整性、跨来源佐证、确认实体和领域置信度分开；正文字符数只进入完整性分项，缺发布时间和重复内容是精选硬门槛。
- 来源质量只按版本化 `source_type` 映射，不写具体媒体名；单篇事件的佐证为 0，pending/rejected 实体不计数。
- 正式数据预演发现 LLM 实体 canary 只覆盖 5 篇，若立即赋权会产生处理覆盖偏差；v1 保留确认实体信号但将权重设为 0，待全量覆盖一致后再配置启用。
- 正式运行 1 以 `2026-08-30T00:00:00+08:00` 为冻结时点，输入 beauty 103 篇，精选 21 篇、全量池非精选 82 篇；其中 6 篇因缺发布时间不能进入精选池。
- 相同输入第二次应用复用 run 1，评分运行仍为 1、逐篇结果仍为 103；精选 API 正式库返回 200，并按分数降序返回当前哈希匹配结果。
- 空库迁移升级/降级/再升级通过，Alembic 到 0020；数据库 `integrity_check=ok`、外键违规为 0，Ruff 和 117 项测试通过。
- 本任务未访问网站、RedFox 或 LLM，新增积分消耗为 0；评分尚未接管日报编排，不会静默改变已发布报告。

## 当前任务：实体候选确认与主库晋升（2026-08-30）

- [x] 定义候选分组、pending/confirmed/rejected 状态和审计字段。
- [x] 增加审查模型、Alembic 迁移及当前 LLM 候选同步。
- [x] 实现 create/link/reject、别名确认和提及解析更新。
- [x] 增加候选审查 API、CLI 和回归测试。
- [x] 迁移正式库并同步 canary 候选。
- [x] 仅确认标题证据清晰的 5 个候选，其他候选保持 pending。
- [x] 验证幂等、数据库完整性和实体 API，补充 Review。

验收标准：模型候选默认不进入实体主库；每次创建、链接或拒绝都有审查记录、操作者和原因；确认操作可以安全重复；同名歧义不得隐式合并；本任务不调用 LLM。

### Review：实体候选确认与主库晋升

- 新增 Alembic 0019 和 `entity_candidate_reviews`：候选按类型与规范表面名称分组，保存 pending/confirmed/rejected、mention ids、证据、操作者、原因和决定时间。
- `create` 创建实体并确认原文名称为别名；`link` 只允许链接类型一致的活动实体；`reject` 不创建实体并将当前提及标记为 rejected。
- 同一决定重复执行返回 reused，冲突决定拒绝；确认后同步更新 mention 当前解析投影、候选状态和处理结果统计，原始 LLM 输出不覆盖。
- 正式库从 61 个未解析位置同步出 46 个候选组。仅确认标题证据明确的 Guerlain、L’Heure Bleue、Off&Relax、贝泰妮、薇诺娜，创建实体 id 4—8 并确认语言别名。
- 当前审查表 confirmed 5、pending 41；对应 LLM 提及 resolved 8、pending 56。主库共 8 个实体、13 个别名。
- 重复确认 Guerlain 返回 reused=true、affected_mentions=0；数据库完整性正常、外键违规为 0，Alembic 无漂移，Ruff 和 114 项测试通过。
- 本任务没有调用 LLM、RedFox 或网站，新增积分消耗为 0。其余候选不会在未确认时进入实体主库、事件聚类或评分信号。

## 当前任务：LLM 实体候选 canary（2026-08-30）

- [x] 核对现有 DeepSeek 客户端、缓存表和密钥注入边界。
- [x] 定义批量实体候选输出、证据校验和缓存指纹。
- [x] 实现最多 5 篇文章的预览/执行 CLI，不自动二次重试消耗积分。
- [x] 用 FakeClient 验证缓存、原文定位、已知实体解析和未知实体保留。
- [x] 选择中英文代表文章并执行一次 Flash canary。
- [x] 核验 Token、结果质量、幂等和数据库完整性，补充 Review。

验收标准：一次请求最多 5 篇；模型只返回原文明确出现且绑定证据的候选；本地校验后才写入实体提及；模型不直接创建或合并实体；相同输入复用缓存。密钥仅从环境变量读取。

### Review：LLM 实体候选 canary

- 新增 `entity-candidates.v1`、有界 evidence 构建、严格 Pydantic/原文子串校验、缓存指纹和本地提及投影；不增加数据库表。
- CLI 最多接受 5 个内容 ID，预览不请求 API；实际选择 34、35、38、93、111，中英文兼有，证据文本共 10918 字符。
- 第一次沙箱内执行在 DNS 连接前失败，没有到达 API、没有写库或计费；获得外网权限后只完成一次批量模型请求，未触发输出修复请求。
- 实际用量为 prompt 5758、completion 4091、total 9849 tokens；返回 49 个不同候选，本地展开为 64 个原文位置。
- 64 个位置的字段切片与 surface 全部一致；3 个解析到 TNT Group、Rabanne、Million Gold for Her Pure Diamonds，61 个未知候选保持 `entity_id=null`。
- 第二次相同执行为 processed 0、skipped 5、cache_hit true，LLM 缓存工件仍为 1；数据库完整性正常、外键违规为 0。
- 人工抽查发现 `NEXT50 2026` 被归为 product、`Science` 被归为 organization 的边界问题；它们未创建实体，不污染主库，下一版应显式跳过无法落入类型体系的活动、栏目或出版物。
- `.env` 被 Git 忽略且权限调整为 600；Ruff、Alembic 无漂移和 111 项测试通过。

## 当前任务：通用实体系统 v1 前两阶段（2026-08-30）

- [x] 定义 `entity-mentions.v1` 和领域无关实体类型。
- [x] 增加实体、别名、处理结果、提及和消歧候选数据模型与迁移。
- [x] 实现配置化种子实体、确定性别名提取和保守消歧。
- [x] 增加实体处理 CLI、列表/详情 API 和按实体筛选内容 API。
- [x] 用本地 fixture 和现有内容验证幂等、证据偏移及歧义不强制合并。
- [x] 运行迁移、全量测试、数据库完整性检查并补充 Review。

验收标准：平台内核不包含美妆专用字段；确定性提取不调用 LLM；每个提及包含原文字段、偏移和证据；唯一别名可解析，歧义别名保持未解析；重复执行不产生新结果。

### Review：通用实体系统 v1 前两阶段

- 新增 Alembic 0018 与 `entities`、`entity_aliases`、`entity_processing_results`、`entity_mentions`、`entity_resolution_candidates` 五张通用表，没有美妆专用列。
- `entity-mentions.v1` 固化字段、原文偏移、证据和当前结果语义；内容或配置变化追加结果，旧结果不覆盖。
- 配置化别名唯一命中才自动解析；fixture 中同一 `Acme` 命中公司和品牌时保持 `entity_id=null`，并产生两个待判断候选。
- 新增 `make extract-entities`、`GET /api/v1/entities`、实体详情接口，以及 `content-items?entity_id=` 过滤。
- 正式库对 103 条 beauty 收录内容完成确定性基线：3 个种子实体、3 个别名、103 个处理结果、11 个原文提及、0 个歧义候选；全部提及来自有明确配置证据的 ContentItem 111。
- 第二次应用为 `processed=0, skipped=103`；Alembic 无漂移，数据库完整性正常、外键违规为 0，Ruff 和 108 项测试通过。
- 全程未访问网络、RedFox 或 LLM，第三方积分消耗为 0。103 个处理结果只表示确定性基线已执行，不代表每篇文章的全部实体已被发现。

## 当前任务：补齐最新内容的中游闭环（2026-08-30）

- [x] 确认数据池与中游缺口，仅 ContentItem 111 尚未处理。
- [x] 执行既有内容处理器，不调用 LLM。
- [x] 同步 beauty 领域归属。
- [x] 重建事件并确认 110 条内容全部具有当前事件成员关系。
- [x] 运行相关测试、数据库完整性检查并补充 Review。

验收标准：110 条 ContentItem 均具有匹配当前内容哈希的处理结果、领域归属和活动事件成员；不触发网站采集、RedFox 或 LLM；历史结果保持可审计。

### Review：补齐最新内容的中游闭环

- `industry-rules.v1` 只处理 ContentItem 111，结果为 beauty 相关；第二次执行处理数为 0。
- beauty 领域同步新增 1 条，既有 109 条跳过；第二次执行为新增 0、更新 0、跳过 110。
- 事件预演输入 110 条、候选比较 1888 对、计划 110 个单篇事件；应用后新增事件 1、复用 109，重复应用复用同一 run 4。
- 当前 110 条内容均有当前处理结果、当前领域归属和活动事件成员；ContentItem 111 对应 Event 110。
- `PRAGMA integrity_check=ok`，外键违规为 0；105 项测试与 Ruff 全部通过。
- 未访问网站，未调用 RedFox 或 LLM，第三方积分消耗为 0。其余 9 个未打通网站按用户决定冻结，后续工作转向实体、价值评分、事件效果、搜索和前端。

## 当前任务：恢复 Premium Beauty News（2026-08-29）

- [x] 以根域现场响应复核 CosmeticsDesign 是否仍在发布。
- [x] 保持 CosmeticsDesign 为 `unavailable`，更正误判记录。
- [x] 确认 Premium Beauty News 的入口、文章规则和正文抽取配置。
- [x] 同步 Premium Beauty News 正式目录配置，保持来源身份不变。
- [x] 补充临时维护恢复的回归测试。
- [x] 执行单源小流量 canary，验证列表、详情、日期和入库。
- [x] 检查重复、运行证据、数据库完整性和来源状态。
- [x] 更新运维审计与任务 Review。

验收标准：Premium Beauty News 通过统一执行通路产生可审计的 CrawlRun、PageSnapshot、RawItem 和 ContentItem；只抓最小样本；CosmeticsDesign 不因历史归档仍可访问而恢复；不调用 LLM、不绕过登录、付费墙或访问挑战。

### Review：恢复 Premium Beauty News

- 采用官网公开英文 RSS，而不是维护期曾失败的首页发现；正式执行通路为 `rss → feed_direct → HTML detail`，来源 id 7 保持不变。
- CrawlRun 419 首次新增 1 条内容；它暴露出详情 JSON-LD 的巴黎无偏移时间被误当 UTC。增加通用来源时区处理后，CrawlRun 420 更新同一 ContentItem，正确时间为 `2026-08-28T20:02:29Z`。
- 两次运行的 RawItem 均保留，ContentItem 111 只有一个当前投影；最新运行抓取 1、更新 1、错误 0。
- 当前数据库 110 条 ContentItem、375 个 RawItem、420 次 CrawlRun；`PRAGMA integrity_check=ok`，外键违规为 0。
- CosmeticsDesign 根域确认已于 2026 年 3 月关闭，继续保持 `unavailable`；历史文章归档不作为恢复证据。
- 全程未调用 LLM 或 RedFox，第三方积分消耗为 0。单次成功只标记“已打通、观察中”，至少跨 3 个发布周期后再评估稳定。

## 当前任务：剩余网站失败来源复核（2026-08-29）

- [x] 从正式数据库与历史报告确认失败来源清单。
- [x] 逐站核对 CrawlRun、PageSnapshot、入口和解析配置。
- [x] 必要时查证公开 RSS、Sitemap、API、robots 和站点状态。
- [x] 区分配置可修、访问受限、动态渲染、停更或不适合作为信源。
- [x] 给出按收益、风险和实施成本排序的处理建议。
- [x] 总结可复用的网站资讯采集经验。

验收标准：每个失败来源都有证据化根因和明确下一动作；不把单次可访问误判为稳定；不绕过 robots、登录、验证码或付费墙；本轮不触发正式批量采集或付费服务。

### Review：剩余网站失败来源复核

- 正式数据库最近非修复运行仍有 10 个未打通来源，但旧报告分类已经过期。
- Premium Beauty News 已从维护状态恢复；现有文章 URL 规则仍适用，可直接做单站复测。
- CosmeticsDesign 的旧文章归档仍可访问，但根域已明确公告 2026 年 3 月关闭；旧 `/Headlines/` 和 RSS 返回 410，不能恢复为日报源。
- Beauty Independent、Beauty Packaging、HAPPI、Cosmetics & Toiletries 的主要问题在访问层/WAF；后三站的 robots 请求本身收到 Cloudflare challenge，不能误记成明确 `Disallow`。
- Beauty Independent 当前 robots 允许通用搜索/引用范围并禁止模型训练，但站内有 Premium 内容；建议仅做公开元数据/引用级 canary。
- Byrdie 条款明确禁止自动抓取/RAG/数据集，未来迹明确 `Disallow: /`，蝉妈妈禁止资讯报告/热点/详情路径；三站继续停爬。
- 刀法研究所域名仍不可达，保留低频健康探测，不纳入日报 SLA。
- 发现 `crawl_strategy=browser` 当前不会选择浏览器执行器：现有注册表没有浏览器引擎，HTML 仍走 `static_http`。这三站过去并未真正完成浏览器采集验收。
- 详细逐站证据、标准流程和实施顺序已更新到 `docs/operations/不能抓取的来源.md`。
- 本轮只做本地数据库/快照审计和少量公开端点探测；未运行正式批量采集、未访问 RedFox、未调用 LLM，第三方积分消耗为 0。

## 当前任务：渠道执行引擎解耦（2026-08-29）

- [x] 盘点集中式抓取器的共享步骤与渠道专属分支。
- [x] 定义统一执行引擎契约、引擎键和注册表。
- [x] 拆分 RSS/Atom、Sitemap、HTML、JSON API 与 RedFox Provider 路由。
- [x] 保持 CrawlRun、PageSnapshot、RawItem 与 ContentItem 行为兼容。
- [x] 补齐引擎选择、配置拒绝和各渠道回归测试。
- [x] 更新架构文档并完成全量验证与缓存归档。

验收标准：`source-pipeline.v1` 的执行引擎可以稳定映射到唯一运行引擎；新增引擎不再修改渠道适配器的大型条件分支；现有采集测试全部通过；不访问公网、不调用 RedFox 或 LLM。

### Review：渠道执行引擎解耦

- 新增 `execution-engine.v1` 和五引擎注册表：`static_http`、`feed_direct`、`sitemap_http`、`json_api`、`provider_api`。
- 渠道适配器只校验渠道、发现方式和引擎兼容性并发起调度；共享抓取器只保留 HTTP、robots、重试、快照、详情循环、计数与统一入库。
- HTML、Sitemap、JSON 列表发现由各自引擎负责；Feed 引擎负责条件请求、增量计划和内联正文；RedFox 引擎负责日期分页、目标日选择和 Provider 详情协议。
- `source-pipeline.v1` 编译结果、来源 API 注册/更新和目录同步都会固化 `parser_config.execution_engine`；显式引擎与渠道或发现方式冲突时返回 422。
- 正式目录 34 个来源已离线同步：27 个 `static_http`、2 个 `feed_direct`、1 个 `sitemap_http`、3 个 `json_api`、1 个 `provider_api`；所有启用来源均有引擎键。
- 71 个已停用历史归档来源没有被重新纳入正式运行配置；化妆品报仍停用且映射为 `provider_api`。
- 共享抓取器已无按 `provider` 或 `discovery_method` 判断的渠道专属分支；新增普通引擎不再需要修改渠道适配器。
- Ruff、102 项后端测试、前端 ESLint、Alembic 无漂移和正式库完整性/外键检查全部通过；数据库内容仍为 418 次运行、373 个 Raw、109 条内容。
- 全程只使用本地 fixture 和数据库；没有访问公网、RedFox 或 LLM，第三方积分消耗为 0。

## 当前任务：冻结采集覆盖日与 RedFox 历史时区修复（2026-08-29）

- [x] 为 CrawlRun 增加 `coverage_date`、`publication_timezone` 和重试溯源。
- [x] 调度首次创建时冻结前一自然日，重试继承原覆盖日。
- [x] 手动触发支持指定日期补采，并限制到日期型来源。
- [x] RedFox 运行只读取冻结覆盖日，不因重试日期变化。
- [x] 以不可变 Raw 新版本离线修复 73 条历史 RedFox 时间。
- [x] 重建受影响日报并完成迁移、测试和数据库验证。

验收标准：同一失败任务跨日重试仍抓原覆盖日；可显式创建指定覆盖日的补采任务；历史修复不覆盖旧 Raw，重复执行不二次减八小时；不访问 RedFox、不调用 LLM。

### Review：冻结采集覆盖日与 RedFox 历史时区修复

- `0017` 为 CrawlRun 增加可空覆盖日、发布时区快照与重试溯源，并约束覆盖日存在时必须有时区；索引支持同源同日运行审计。
- 调度在创建时冻结来源时区前一自然日；手动日期型补采支持显式历史日；重试继承原上下文。已有不同上下文活动任务返回 409，不再误复用。
- RedFox 执行器只读 CrawlRun 冻结值；来源配置修改和跨日排队不会改变目标日。RedFox 配置注册时校验显式有效 IANA 时区。
- 历史修复 dry-run 精确命中 73 条、72 个来源；应用后新增 73 个 Raw 版本，旧 300 个 Raw 零修改，109 条内容身份和语义字段零变化，二次 dry-run 命中 0。
- 事件层离线重建后仍为 109 个活动事件；8 月 27 日日报从错误的 4 条修正为 3 条，复用现有单篇 LLM 工件与既有栏目位置，没有新增模型调用。
- 显式覆盖日报的出版日固定为覆盖日加一天；最终 HTML 为“2026 年 08 月 28 日出版 / 覆盖 2026 年 08 月 27 日发布内容”，无 CST、固定栏目或 AI 说明。
- Ruff、97 项后端测试、前端 ESLint、空库 Alembic 完整升级/降级/再升级、正式库完整性和外键检查全部通过；正式库为 105 个来源、418 次运行、373 个 Raw、109 条内容。
- 修复前数据库恢复点与说明位于 `archive/2026-08-29-coverage-timezone-repair/`；全程未访问 RedFox、未调用 LLM，第三方积分消耗为 0。

## 当前任务：RedFox 置顶与广告证据过滤修正（2026-08-28）

- [x] 日期仍为北京时间前一自然日硬约束。
- [x] 仅按明确布尔字段、类型或标签排除置顶和广告。
- [x] 禁止使用 `orderNum` 和标题关键词作推断。
- [x] 补齐明确命中、标题误判保护和普通文章测试。
- [x] 同步配置、契约与运维说明并完成全量验证。

验收标准：前一日文章中，有明确置顶/广告结构化证据的条目被排除；只有“广告、推广”等标题文字或 `orderNum=0` 的文章不被误杀；全程不访问 RedFox。

### Review：RedFox 置顶与广告证据过滤修正

- 日期过滤仍先执行，只处理来源时区内前一自然日；随后才应用置顶/广告证据过滤。
- 可信证据包括明确布尔字段、广告类型，以及 `tags/tagList/labels/categories/contentKeywords` 中的精确置顶、广告、推广或赞助标签。
- `orderNum` 不参与判断；标题出现“广告”“推广”等文字也不会触发排除，避免把行业分析文章误杀。
- 正式配置显式开启 `exclude_explicit_pinned` 与 `exclude_explicit_advertising`，来源同步到数据库后仍保持停用。
- 明确置顶布尔值和广告标签命中测试通过，`orderNum=0 + 标题含广告` 的无标签文章保留测试通过。
- Ruff、90 项后端测试、Alembic 无漂移和 SQLite 完整性检查通过；未访问 RedFox，积分消耗为 0。

## 当前任务：RedFox 前一自然日采集（2026-08-28）

- [x] 删除正式配置中的置顶、广告和固定顺位选择规则。
- [x] 按来源时区筛选运行日前一自然日发布的全部列表条目。
- [x] 自动翻页直到越过目标日期，分页未闭合时明确失败而非漏采。
- [x] 将无时区的 RedFox `publishTime` 按 `Asia/Shanghai` 解释后写入 UTC。
- [x] 覆盖多篇、空日、午夜边界、分页与旧配置拒绝测试。
- [x] 更新正式来源配置、数据契约和运维文档并完成离线验证。

验收标准：正式 RedFox 来源不再按位置选文；每次运行只处理北京时间前一自然日的所有已发现文章；列表第一页不足以覆盖日期边界时继续分页；未验证完整边界时运行失败；不访问 RedFox 或消耗积分。

### Review：RedFox 前一自然日采集

- 已由本地真实快照确认 RedFox 列表条目包含无时区 `publishTime`、按时间倒序、单页 20 条并提供 `total`，具备按发布日期和分页边界采集的必要字段。
- 正式适配器不再读取 `orderNum`，也不再接受 `skip_first_article`、`skip_ad_titles` 或 `skip_pinned`；置顶和广告只要发布日期命中，也按原始事实进入数据池，由中游决定是否收录。
- 目标日由 `CrawlRun.started_at` 转为 `publication_timezone` 后减一天得到；当前 RedFox 来源固定为 `Asia/Shanghai`。目标日零文章是健康成功。
- 列表持续按 offset 翻页，直到出现早于目标日的条目或到达 `total`；达到 `max_listing_pages` 仍未闭合时失败，避免静默漏采。
- 无时区 `publishTime` 现在按北京时间解释并转为 UTC 入库，修复未来文章被错误加八小时、跨日进入日报的问题；历史 73 条公众号内容尚未自动改写。
- 化妆品报正式来源配置已同步到数据库且保持停用；其他公众号仍是停用的历史归档来源，没有恢复离线导入或批量在线抓取。
- 本地双页模拟验证只抓目标日两篇并排除当天和更早文章；90 项后端测试、Ruff、Alembic 无漂移、SQLite 完整性和前端 ESLint 均通过。全程未访问 RedFox 或调用 LLM，积分消耗为 0。

## 当前任务：RSS/Atom 真增量同步（2026-08-28）

- [x] 新增独立的来源同步状态模型与迁移，不改动 `article.v1.1`。
- [x] 对直接 RSS/Atom GET 请求发送 `If-None-Match` / `If-Modified-Since`。
- [x] 将 HTTP 304 记录为健康的 `unchanged` 运行并保留响应证据。
- [x] 使用 GUID/Atom ID、条目指纹与最近窗口筛选新增/更新条目。
- [x] 仅在整批处理成功后推进 checkpoint，失败时保留旧状态。
- [x] 以本地模拟响应覆盖首次同步、304、重叠更新和失败回滚。
- [x] 更新架构/运维文档并完成 Ruff、pytest、Alembic 与数据库校验。

验收标准：RSS/Atom 来源首次同步后保存校验器和条目窗口；再次请求可返回 304 而不重复解析；Feed 中新条目或旧条目内容变化可被识别；解析或入库失败不会推进 checkpoint；全程不访问公网或调用 LLM。

### Review：RSS/Atom 真增量同步

- 新增 `source_sync_states`，保存版本化 ETag、Last-Modified、GUID/Atom ID + 条目指纹窗口、水位和最后提交运行；`article.v1.1` 未改动。
- 条件头只发往直接 RSS/Atom 的 Feed GET，不会进入 robots、详情页、POST Feed 或第三方 Provider 请求。
- HTTP 304 保存空正文 PageSnapshot，运行状态为健康的 `unchanged`；日报来源健康统计也将其视为健康。
- 新增/变化条目优先于最近重叠条目；当变化数超过单轮上限时暂不接纳新 validator，逐轮排空 backlog 后再提交，避免 304 漏采。
- 解析失败、内容拒绝或详情错误均不推进 checkpoint；全文 Feed 的内容写入与 checkpoint 使用同一最终事务，链接 Feed 的既有部分入库依靠幂等重放。
- 本地模拟验证首次 200、条件头、304、同 GUID 更新、失败回滚、Atom ID、规范化链接、backlog 优先级和调度健康语义；没有访问公网或调用 LLM。
- Ruff 通过；88 项后端测试通过；Alembic 空库升级/降级/再升级和无漂移检查通过；正式库升级到 `0016`，完整性检查通过，仍为 105 个来源、109 条内容，同步状态为 0（尚未联网运行真实 Feed）。前端 ESLint 通过。

## 当前任务：来源结构探测与 Pipeline 契约（2026-08-28）

- [x] 定义并校验 `source-probe-result.v1` 和 `source-pipeline.v1`。
- [x] 实现不写数据库的 HTML、RSS/Atom、Sitemap、JSON 与访问阻断检测。
- [x] 根据检测证据生成有序、可解释的执行 Pipeline 建议。
- [x] 提供只读本机 CLI；因匿名 API 无认证，暂不开放联网 Probe API。
- [x] 限制协议、端口、私网/DNS、robots、重定向、响应大小和超时。
- [x] 使用本地 fixture 与 MockTransport 覆盖主要格式、组合路线与安全拒绝场景。
- [x] 更新架构和进度文档，完成 Ruff、pytest、迁移和数据库验证。

验收标准：给定一个公开 URL 或本地测试响应，可以在不注册、不启用、不写数据库的情况下输出版本化探测结果；Pipeline 建议不包含公众号离线导入、公众号浏览器采集或 WeWe RSS；测试不访问公网和付费服务。

### Review：来源结构探测与 Pipeline 契约

- 新增纯函数探测器，输入只有 URL、状态、Content-Type、正文和 robots 事实；输出不含正文、Cookie、Authorization 或敏感响应头。
- RSS 与 Atom 分开识别；完整、摘要和仅链接 Feed 分流；Sitemap index 不会被误当文章集合。
- HTML 可识别官方 Feed 声明、列表/文章结构及 HTTP 200 challenge；声明的 Feed 保持未验证，不能自动晋升。
- JSON 只识别候选数组路径，不自动冻结字段映射；JSON 仍是传输方式，不把来源载体硬判为数据供应商。
- Pipeline 推荐始终为 `draft`；只有显式 `verified` 才能通过兼容函数投影为当前 `parser_config`。
- 联网 CLI 仅允许公开 HTTP(S) 默认端口，逐跳校验 DNS/IP，手动限制重定向，先查 robots，并限制超时与解压后响应大小。
- 鉴于 API 当前没有管理认证，没有增加匿名联网 Probe API；同时禁止通过来源注册 API 注入 `request_headers_env` 等密钥请求字段，目录内受控 RedFox 配置不受影响。
- Ruff 通过；84 项后端测试通过；Alembic 无漂移；SQLite integrity/foreign key 检查通过；数据库仍为 105 个来源、109 条内容。

## 当前任务：渠道架构收敛与项目归档（2026-08-28）

- [x] 从正式架构和运行入口移除公众号离线导入、公众号浏览器采集、WeWe RSS。
- [x] 保留 RedFox API 适配器，但维持公众号在线来源停用，不把历史数据删除。
- [x] 建立 `docs/` 正式文档层和单一 `archive/2026-08-28-channel-cleanup/` 存档目录。
- [x] 归档旧公众号工具、一次性升级工具、研究材料、过程报告和旧架构图，并写清单与恢复边界。
- [x] 更新 README、数据契约、进度说明、Makefile 和忽略规则。
- [x] 运行源码引用扫描、Ruff、pytest、Alembic 与数据库完整性检查。

验收标准：正式运行目录中不再存在三条被移除路径的可执行入口或能力声明；所有历史文件仍可在统一存档中追溯；稳定的 HTML/RSS/Sitemap/JSON API/RedFox API 与日报链路测试通过。

### Review：渠道架构收敛与项目归档

- 正式采集口径收敛为 HTML、RSS/Atom、Sitemap、JSON API 和保留但默认停用的 RedFox API；WeWe RSS 无实现，已从现行说明删除。
- 公众号离线导入服务/CLI/测试、浏览器与公众号后台脚本、fakeid/批跑工具、一次性契约升级工具全部迁入统一存档。
- 历史公众号内容和来源外键未删除；数据库仍为 109 条内容、105 个来源，其中 72 个 `third_party_feed` 来源全部停用。
- 根目录文档重组为 `docs/architecture`、`docs/contracts`、`docs/operations`；含旧路径的 SVG 已作为历史架构归档。
- 阶段性站点报告、LLM 前日报、研究材料、系统元数据及正式源码缓存已离开运行目录；`output/` 只保留当前日报和最终验收基线。
- 存档共 151 个文件、约 1.8 MB；敏感存档由 `.gitignore` 排除并设为 `0600`。
- Ruff 通过；正式后端 61 项测试通过；Alembic 无漂移；SQLite integrity/foreign key 检查通过；前端 ESLint 通过。

## 当前任务：日报元信息与数据卡精简（2026-08-28）

- [x] 刊头只保留出版日与覆盖发布日，删除来源截止时间和数量信息。
- [x] 删除四个统计卡片，保持编辑简报而非仪表盘风格。
- [x] 删除每条内容的 CST 发布时间，保留必要的多来源证据提示。
- [x] 使用 LLM 缓存离线重建并完成测试与最终 HTML 静态验收。

### Review：日报元信息与数据卡精简

- 刊头现仅有“2026 年 08 月 28 日出版”和“覆盖 2026 年 08 月 27 日发布内容”，数量与采集截止时间均删除。
- 四个统计卡片整段删除；栏目和标签继续承担信息导航，不以其他仪表盘数字替换。
- 单条内容不再显示 `HH:MM CST`；多来源事件仍可按需显示来源数量证据。
- 页脚精简为原文版权说明，页面中已无 `CST`、数据截止时间、近 7 日卡片或固定栏目残留。
- `--llm-cache-only` 离线命中，没有新增模型调用；Ruff 与 65 项测试通过，最终 HTML 保留 4 条故事。
- 应用安全策略禁止自动控制当前 `file://` 标签，未绕过限制；最终文件已在用户现有标签中，可直接目视确认。

## 当前任务：两阶段 LLM 日报核心（2026-08-28）

- [x] 定义单篇 `content_editorial.zh.v2` 与稳定证据引用契约。
- [x] 实现单篇指纹缓存、小批生成与逐篇落库。
- [x] 实现只消费单篇工件的 `daily_edition.zh.v1` 整期编排契约。
- [x] 扩展 LLM 缓存指纹字段并增加 Alembic `0015`。
- [x] 补齐定向测试，运行 Ruff、pytest 和迁移验证。

验收标准：单篇结果可跨日报复用；证据 ref 可稳定追溯到标准化内容位置；整期输出不读取原始正文，且每个故事在 sections 中恰好出现一次；DeepSeek 请求仍不设 `max_tokens`。

### Review：两阶段 LLM 日报核心

- 单篇输出拆为中文标题、逐句 summary claim 与领域规则中的规范标签；每项都绑定由代码生成的字段、字符区间和片段 hash 引用。
- 单篇缓存键包含内容输入、Prompt、JSON Schema、校验器、Provider/模型与生成参数指纹；小批请求成功后逐篇落库。
- 整期编排只读单篇工件、事件成员关系和领域 policy；校验 sections 对当期故事集合恰好覆盖一次。
- `0015` 在 SQLite 空库完整升级、降级回 `0014`、再升级均通过；Alembic check 无漂移。
- Ruff 通过；后端 65 项测试全部通过（仅有既有 Starlette/httpx 弃用警告）。

## 当前任务：冻结统一内容契约 article.v1.1（2026-08-27）

- [x] 明确 `article.v1.1` 字段、默认值、追溯关系和向后兼容边界。
- [x] 扩展契约、数据库模型、API schema 与各渠道提取映射。
- [x] 增加 Alembic 迁移和既有 `content_items` 本地回填。
- [x] 为 HTML、RSS、JSON API、RedFox/公众号建立契约测试。
- [x] 迁移当前数据库并验证所有内容池记录符合 v1.1。
- [x] 运行 Ruff、pytest、Alembic、SQLite 完整性检查并更新文档。

验收标准：所有新采集渠道输出同一个 `article.v1.1`；现有 109 条最新内容视图完成本地升级；任一内容可追溯到来源、运行、原始快照和外部条目标识；不修改历史原始版本，不产生网络或积分消耗。

### Review：article.v1.1

- 契约新增来源/内容外部 ID、渠道、供应商、发现地址、来源更新时间、跨语言字数、媒体与可空完整性质量字段。
- 运行 ID 和快照 ID 作为 API 溯源信封从 RawItem 返回，不参与语义 payload hash。
- 外部 ID 绑定兼容旧 URL identity；规范化 URL 回退修复微信历史长链的编码/fragment 差异。
- 本地升级为109条最新内容各追加 v1.1 Raw 版本，旧191条 Raw 保持不变；最终为109条内容、300个 Raw 版本。
- 72条内容绑定 `workUuid`，72个公众号来源绑定 fakeid；109条当前 Raw 与内容投影均为 v1.1。
- 升级中发现并纠正1条由旧微信 canonical 未规范化造成的派生重复；删除的错误投影和孤立派生 Raw 可从 `/tmp/navigate_before_contract_duplicate_repair_20260827.db` 恢复。
- Ruff、41项测试、前端 ESLint、Alembic `check`、空库升级和 SQLite 完整性检查通过；网络请求和积分消耗为0。

## 当前任务：低积分模式下推进后续链路（2026-08-27）

硬约束：本轮只消费已有本地数据；不调用 RedFox 列表/详情接口，不重新请求 `meta_only` / `error`，不调用付费 AI。

- [x] 设计本地 RedFox JSONL 的来源映射、幂等入库和零积分防护。
- [x] 实现可复用的离线导入服务与 CLI，并补单元测试。
- [x] 导入 72 条 `ok`，验证重复运行不新增重复内容，并完成中游加工。
- [x] 让中游加工按 `content_hash` 感知内容更新并可安全重算。
- [x] 评估并落地跨来源严格重复的最小数据基础，不引入付费模型。
- [x] 运行 Ruff、pytest、迁移/数据库一致性检查，记录实际结果与后续边界。

验收标准：不产生第三方积分消耗；现有 72 条正文全部可追溯、幂等进入统一内容层；内容更新后加工结果不会陈旧；下一阶段可在此基础上做事件聚类。

### Review：低积分模式推进

- 纯离线读取 206 行红狐归档，只导入 72 条 `ok`；没有调用任何第三方接口，积分消耗为 0。
- 新增 71 个稳定公众号来源并复用 `huazhuangpinbao_wechat`；新增来源与现有在线 RedFox 来源全部停用。最终公众号内容为 73 条，总内容为 109 条。
- 离线导入默认 dry-run；最终实际复跑结果 `new=0 / updated=0 / skipped=72`，证明当前契约下幂等。
- `0010` 增加加工输入哈希。当前 109 条加工结果均与内容哈希一致；陈旧结果不进入 API 展示或筛选。
- `0011` 增加 `duplicate_of_id`、`duplicate_rule` 和索引；严格规则只认跨来源、正文至少 200 字、完整内容哈希相同。当前没有命中重复。
- Ruff、38 项后端测试、前端 ESLint、Alembic `check`、空库升级、SQLite integrity/foreign key 均通过。
- 迁移前数据库副本保存在 `/tmp/navigate_after_offline_wechat_import_20260827.db`；这是导入完成、结构升级之前的临时恢复点。

## 当前任务：项目架构与进度审阅（2026-08-27）

- [x] 通读 `进度与注意事项.md`，提取现状、约束、风险与下一步。
- [x] 核对 README、目录、后端/前端入口、数据模型与测试，形成架构图景。
- [x] 对照 Git 状态、数据库/测试事实验证文档中的关键进度声明。
- [x] 汇总架构、已完成项、未完成项、注意事项和建议优先级。

验收标准：输出能让接手者快速理解系统分层、关键链路、真实完成度、当前风险和最合理的下一步；不修改业务代码。

### Review：项目架构与进度审阅

- 当前是“配置驱动的统一采集后端 MVP”：来源目录、四类 HTTP 渠道、抓取快照、`article.v1`、原始版本、最新内容视图、中游行业相关性、频率调度与熔断均已落地；产品层尚未开始。
- 数据流为 `sites.json → sources → crawl_runs → page_snapshots → raw_items → content_items → content_processing_results → 查询 API`。公众号 206 号红狐试跑仍主要停留在 JSONL，正式库仅有 1 条公众号正文。
- 只读核验：34 个来源、130 次运行、226 个快照、47 个原始版本、37 条内容、36 条加工结果；网站最新状态为 23/33 成功，另 8 个失败、2 个无运行记录。
- 配置与试跑数字和 `进度与注意事项.md` 一致：206 个 fakeid；红狐结果为 72 `ok`、94 `meta_only`、10 `list_3203`、30 `error`。
- 自动验证：Ruff 通过，后端 31 项测试通过（有 1 条 Starlette TestClient/httpx 弃用警告），Alembic 位于 `0009` head，前端 ESLint 通过。
- 主要缺口：公众号统一入库与可调度化、加工结果随内容版本重算、跨来源去重/事件聚类、AI/评分/搜索/订阅/日报、前端、权限和生产运维。
- 主要风险：API 无鉴权且可触发任意 URL 抓取；任务依赖进程内 BackgroundTasks + SQLite；快照正文全量存 SQLite；统一抓取器职责过重；当前目录没有 `.git`，无法核验历史或未提交变更。
- 文档漂移：README 仍写 33 个来源；本计划部分“未做”项（基础 URL 规范化、正文提取、RSS/API 契约）实际上已实现。后续应按“红狐入库 → 加工重算语义 → 跨源去重/事件 → 安全与持久任务 → 搜索/产品层”推进。

> 目标：逐段打通“来源注册 → 定时采集 → 有效性校验 → 原始记录 → 统一内容 → 去重/聚类/AI 加工 → 全量池/精选池 → 搜索/热点/订阅/日报”的完整闭环。

## 总体实施原则

- 先用一个真实行业网站打通最小闭环，再扩展 RSS、API 和公众号渠道。
- 原始数据与加工结果分层保存，任何加工步骤都可以重新执行。
- 所有采集和加工任务必须幂等，同一文章重复运行不会产生重复数据。
- 每个阶段均以可运行演示、自动测试和可观测记录作为完成标准。
- 首期先做单机 MVP；接口和数据模型保留迁移到 PostgreSQL、队列和多实例部署的空间。

## 推荐技术基线（实施前确认）

- 后端与任务：Python 3.12、FastAPI、SQLAlchemy、Alembic。
- MVP 数据库：SQLite；进入多人或生产环境时切换 PostgreSQL。
- 调度：MVP 使用 APScheduler；需要分布式执行时再切换 Redis + RQ/Celery。
- 前端：Next.js + TypeScript；首期只实现必要的信息流、筛选和详情页。
- AI：通过统一 Provider 接口调用模型，摘要、分类、实体识别分别保存版本和状态。
- 本地运行：Docker Compose 或一条开发启动命令，密钥全部通过 `.env` 注入。

## 里程碑 0：工程骨架与安全基线

- [x] 确认技术基线及首期部署方式（首期仅后端与 SQLite，本地运行）。
- [ ] 建立 `backend/`、`frontend/`、`tests/`、`scripts/`、`docs/` 目录。
- [x] 建立环境变量模板 `.env.example`，把所有密钥移出项目文件。
- [x] 加入 `.gitignore`、配置加载和存活/就绪健康检查。
- [x] 初始化数据库迁移、开发启动命令和本地检查命令。

验收标准：新环境按 README 可启动后端、前端和数据库；健康检查通过；仓库中不存在明文密钥。

## 里程碑 1：网站抓取最小闭环

- [x] 定义统一来源模型 `sources`：名称、渠道类型、列表页地址、抓取频率、发现规则、解析规则、启停状态。
- [x] 定义采集运行模型 `crawl_runs`：开始/结束时间、状态、数量、错误摘要。
- [x] 定义原始记录模型 `raw_items`：来源、原始载荷、抓取时间、内容指纹。
- [x] 定义统一内容模型 `content_items`：标题、URL、来源、发布时间、正文、作者、语言等。
- [x] 实现来源注册、列表、启停和手动触发 API。
- [x] 实现通用网站采集器：列表页发现文章链接、站内 URL 过滤、文章页下载、正文与元数据提取。
- [x] 为 BeautyMatter 建立首个真实适配器，并过滤会员专享卡片。
- [x] 保存列表页和文章页原始响应，支持重放与重新解析。
- [x] 增加 robots.txt 检查、请求间隔、超时、User-Agent 和单站点串行访问。
- [x] 实现基于来源 ID + 规范化 URL 的数据库约束与幂等写入。
- [x] 实现来源、抓取运行、原始页面和统一内容的查询 API；前端页面后置。

验收标准：注册一个网站来源后可手动触发；能从列表页发现并抓取文章正文；原始页面和统一内容均可查询；重复运行无重复文章；失败页面不影响同批其他文章。

## 当前实施：文章标签 + 调度可观测性

边界：采集层只提取并保存标签，不按美妆/护肤丢弃文章；行业相关性仍在中游 `industry_rules` 判定。

- [x] 从 HTML / RSS / JSON 提取文章 tag，写入 `article.v1.topics`。
- [x] 中游用文章 tag 作为行业命中信号（`tag_match`），无 tag 时仍走正文关键词。
- [x] 调度器增加单来源熔断：连续失败 3 次后冷却 6 小时，不阻塞其他来源。
- [x] 来源列表返回健康摘要：最近状态、连续失败、熔断、最近错误。
- [x] 抓取结束写结构化日志；失败/部分失败任务可重跑；运行耗时由起止时间计算。

验收：离线 fixture 能抽出 tag；仅有护肤/美妆 tag 的综合媒体文章可被中游标为相关；熔断来源不会进入到期队列；`make check` 通过。

## 里程碑 2：调度、校验与可观测性

- [x] 根据来源频率自动生成抓取任务。
- [x] 增加 HTTP 状态、响应类型、更新时间和必填字段校验。
- [x] 增加失败重试、超时、退避和单来源熔断策略。
- [x] 保存每次抓取的成功数、跳过数、失败数和耗时。
- [x] 增加来源健康状态和最近错误展示。
- [x] 增加结构化日志与失败任务重跑入口。

验收标准：调度器可连续运行；模拟超时、错误响应和字段缺失时有明确记录并按策略重试；单个坏来源不会阻塞其他来源。

## 里程碑 3：去重、正文和事件聚类

- [ ] 实现 URL 规范化，去除常见追踪参数。
- [ ] 实现标题/正文指纹及严格重复判定。
- [ ] 增加正文提取与正文质量检查。
- [ ] 实现相似文章候选召回和事件聚类。
- [ ] 保存重复关系、聚类 ID、算法版本和置信度。
- [ ] 提供人工合并、拆分和纠错入口。

验收标准：同一新闻的不同转载不会重复占据信息流；聚类结果可解释、可人工纠正且可重算。

## 里程碑 4：分类、实体识别与 AI 摘要

- [ ] 建立美妆行业分类体系与关键词词典。
- [ ] 提取品牌、公司、人物、产品、成分、地区等实体。
- [ ] 为单篇内容和事件簇分别生成摘要。
- [ ] 保存 Prompt、模型、版本、输入哈希、输出和费用信息。
- [ ] 增加失败重试、缓存和人工重新生成能力。
- [ ] 建立小规模人工标注集，评估分类与摘要质量。

验收标准：新内容能稳定获得分类、实体和摘要；相同输入不会重复付费；错误输出可追溯并重新生成。

## 里程碑 5：评分、全量池、精选池与热点榜

- [ ] 定义质量分、时效分、来源权重、传播热度和相关性评分。
- [ ] 实现关键词包含、排除和主题匹配规则。
- [ ] 全量池保留所有有效内容。
- [ ] 精选池由规则初筛并支持人工复核。
- [ ] 热点榜按事件聚合，避免同一事件的多篇转载重复霸榜。
- [ ] 为评分结果保存组成项和版本，支持解释与回放。

验收标准：任一条精选或热点结果都能解释入选原因；修改规则后可重算且不会破坏原始内容。

## 里程碑 6：扩展采集渠道

按风险从低到高依次实施，每种渠道都复用里程碑 1、2 的统一接口和验收用例。

- [ ] RSS/Atom 适配器：复用统一来源和内容模型。
- [ ] API/热榜适配器：先接入一个公开或商业 API。
- [ ] 公众号适配器：整合现有红狐抓取脚本并移除明文密钥。
- [ ] 对各渠道补充限流、缓存、反爬和独立解析规则。
- [ ] 建立适配器契约测试，确保统一输出结构。

验收标准：每种渠道至少有一个真实来源稳定进入同一 `content_items` 管道；新增适配器不需要改动下游加工逻辑。

## 里程碑 7：产品页面、搜索与主题

- [ ] 完善全部动态：分页、来源、时间、分类和关键词筛选。
- [ ] 实现内容/事件详情及原文跳转。
- [ ] 实现全文搜索和实体搜索。
- [ ] 实现热点榜、精选资讯和主题页。
- [ ] 实现收藏和基础后台管理。
- [ ] 展示来源、采集时间、AI 处理状态和评分解释。

验收标准：用户可从信息流完成筛选、搜索、查看事件、打开原文和收藏的完整操作。

## 里程碑 8：用户订阅与日报

- [ ] 定义用户订阅规则：关键词、主题、品牌、来源、排除词和频率。
- [ ] 实现订阅规则预览，显示规则将命中的历史内容。
- [ ] 生成用户级日报，按事件去重并附来源链接。
- [ ] 增加定时生成、历史归档和失败重试。
- [ ] 首期提供站内日报；邮件、飞书等推送作为后续渠道。

验收标准：用户可创建规则、预览结果，并在固定时间获得内容正确、无明显重复的日报。

## 里程碑 9：生产化与质量保障

- [ ] 为采集器、标准化、去重、评分和订阅规则补齐测试。
- [ ] 建立数据库备份、恢复和数据保留策略。
- [ ] 增加指标、告警、任务积压与成本监控。
- [ ] 加入权限、审计、速率限制和敏感信息保护。
- [ ] 进行容量、故障恢复和端到端回归测试。
- [ ] 编写部署、运维和新增来源手册。

验收标准：关键故障可告警、可恢复；备份恢复经过演练；端到端回归覆盖核心链路。

## 第一轮实施范围

第一轮只执行里程碑 0 和里程碑 1，目标是形成如下可验证闭环：

`注册一个网站来源 → 抓取列表页 → 发现文章链接 → 抓取文章页 → 保存原始响应 → 提取正文 → 标准化入库 → 幂等去重 → API 查询`

第一轮暂不加入 Web 前端、AI、复杂聚类、公众号浏览器校验、多用户权限和外部推送。

## 当前实施：统一采集渠道层

- [x] 建立渠道适配器注册表，统一分发 `web`、`rss`、`api`、`third_party_feed`。
- [x] 让来源注册表显式保存渠道、入口地址、抓取频率和渠道规则。
- [x] 实现到期来源查询和调度触发，避免未到频率重复抓取。
- [x] 保持 HTTP 状态、响应时间/更新时间和必要字段校验，并保存原始 HTML、RSS 或 JSON。
- [x] 所有渠道复用 `article.v1` 标准化与幂等入库，不在采集层做行业筛选。
- [x] 为第三方 Feed 建立合规适配器边界；没有服务地址或授权时返回明确配置错误。
- [x] 增加迁移、API、CLI 和离线测试，并以至少一个真实来源做 smoke test。

验收标准：注册来源后可按频率选出到期任务，由对应适配器抓取并保存原始响应，最终进入同一内容表；重复调度不会创建并行任务，未到期来源不会触发。

### Review：统一采集渠道层

- 现有 33 个目录来源已迁移为显式渠道：网页 28、RSS 2、公开 API 3；第三方 Feed 当前无已授权来源，但注册、鉴权环境变量和 RSS/JSON 两种响应契约已实现。
- 新增单次到期执行与常驻轮询调度；按来源频率判断，跳过停用、规则停爬、不可用、未到期和活动任务，并回收超过一小时未结束的任务。
- `page_snapshots` 现在保存请求方法、HTTP 状态、白名单响应头、原始正文和错误信息；`raw_items.page_snapshot_id` 可回溯对应原始响应。
- 网页 BeautyMatter、RSS 36氪、API 艾瑞咨询真实复测均成功，重复内容按预期跳过。
- Ruff、26 项测试、Alembic `check` 和空库从 `0001` 升级到 `0009` 均通过。

## 关键数据边界

- `raw_items` 保存外部世界的原始事实，不被后续加工覆盖。
- `content_items` 保存标准化后的文章级内容。
- `events` 保存多篇文章聚合后的事件级内容。
- AI 输出和评分使用独立版本化记录，不直接覆盖文章正文。
- 全量池与精选池优先使用状态/关系表达，不复制完整内容。

## Review

- 计划按依赖关系排序：基础设施 → RSS 闭环 → 稳定性 → 内容加工 → 产品能力 → 扩展渠道 → 用户日报 → 生产化。
- 根据产品优先级，普通网站抓取被选为首个渠道；RSS 调整到统一网站管道稳定之后。
- 多渠道扩展安排在统一管道稳定之后，避免为每个来源重复建设存储和加工逻辑。
- 下一步：完成网站抓取器、首个真实网站适配器与离线 fixture 验收。
- 2026-08-27 实测：BeautyMatter 首轮抓取 10 篇，新增 10、错误 0；第二轮抓取相同 10 篇，新增 0、跳过 10，内容总数保持 10。
- 两轮共保存 22 份页面快照、10 个不可变原始条目版本和 10 个统一内容条目。
- 离线 fixture 覆盖链接发现过滤、会员卡片排除、robots 规则、正文提取、URL 规范化、重复抓取和内容版本更新；6 项测试通过。
- 空数据库迁移到 `0001_initial` 并重复执行通过。
- 前端开发已按用户优先级暂停，下一步继续增强网站采集可靠性和增加第二个站点适配器。
- 2026-08-27 扩展为 33 个去重网站来源：英文 18 个、中文 15 个；修正化妆品报、青眼、未来迹等错误或迁移入口，并为来源增加语言、地区、类型、访问级别和标签。
- 已落地 `article.v1` 统一数据契约，包含来源标签、语言、正文、摘要、主题、赞助/盘点标志和稳定内容哈希；中英文共用一条标准化链路。
- 已实现 HTML 与 RSS/Atom 发现、重试退避、站点级文章选择器、目录批量运行、指定来源复测和最近结果汇总。行业/事件双重预筛已从采集层撤销。
- 合规联网首轮覆盖全部 33 个来源；早期报告中的“行业过滤”统计属于错误分层，仅保留为历史试爬记录，不再作为现行采集指标。
- 首轮及修正复测共获得 9 条最新契约样本，其中英文 7 条、中文 2 条；已删除 WWD 跨频道误采样本，并用迁移回填早期 BeautyMatter 样本的英文标签。
- 受限来源已按原因拆分：403/robots、订阅或元数据、客户端渲染/浏览器适配、入口失效、暂不可用；没有绕过登录、付费墙或 robots.txt。
- 技术路线已先参考 `参考项目与AIHOT清单.md`，并检查 ForgeRSS、NewsNow 的公共模型、适配器注册和多引擎回退思路；本项目采用独立实现的“目录 + 公共引擎 + 站点配置 + 统一契约”。
- 自动检查结果：Ruff 通过，14 项测试通过，Alembic 已升级到最新迁移；当前汇总报告位于 `output/site_crawl_report_20260827_115907.md`。
- C2CC、青眼通过公开 JSON 列表接口发现文章并抓取公开详情；品观、魔镜市场情报通过 HTML 规则打通；艾瑞以公开 API 采集报告标题、摘要、日期和原文链接，不下载受限 PDF。
- 已补齐 Feed 正文、JSON API、Sitemap 三种发现/提取路径，36氪、The Klog、Global Cosmetics News 等不再依赖错误入口。
- 2026-08-27 全量复跑 33 个来源：23 个当前成功抓取，5 个被 403/站点维护阻挡，3 个按 robots 或站点条款停爬，2 个站点关闭或持续不可用；详见 `output/source_validation_summary_20260827.md`。
- 当前统一库有 36 条内容：英文 23、中文 13；形式为 article 28、news 7、report 1。`industry-rules.v1` 中游加工结果为相关 29、不相关 7。
- 自动检查结果更新为 Ruff 通过、18 项测试通过；最终最近状态报告为 `output/site_crawl_report_20260827_132008.md`。
- 架构纠正：采集层不再按美妆/品牌关键词筛文章，只按文章、新闻、报告等内容形式发现和校验；行业相关性迁移到统一内容库之后的中游加工层，并以独立、可版本化结果保存。
- 分层复测：界面新闻·消费的一篇非美妆新闻先完整进入原始层和统一内容库，再由 `industry-rules.v1` 标记为 `is_relevant=false / no_industry_match`，证明中游筛选不会造成采集丢失。
- 2026-08-27：文章 tag 写入 `content_items.topics`，采集层不按 tag 丢弃。中游对综合媒体可用 tag 命中美妆/护肤（`tag_match`）；无 tag 时仍用正文关键词。
- 调度熔断：连续失败 3 次冷却 6 小时；`GET /api/v1/sources` 返回 `last_run_status` / `consecutive_failures` / `circuit_open`；`POST /api/v1/crawl-runs/{id}/retry` 重跑失败任务。
- 自动检查：Ruff 通过，28 项测试通过。
# 当前实施：通用事件聚类 v1 + 最新数据每日报告

目标：不把美妆写入平台内核，先建立可供任意行业复用的事件模型和本地、可解释聚类；随后使用当前数据库最新内容投影生成一份真实 HTML 每日报告。全程不调用 LLM、红狐或其他付费 API。

- [x] 审查现有模型、迁移、API、最新数据与参考页面；记录无法访问的参考边界。
- [x] 增加通用 `domains` / `content_domain_assignments`，把现有美妆判断降级为首个可版本化领域结果，而不是平台级布尔真值。
- [x] 定义领域无关的 `events` / `event_members` / `event_cluster_runs`，保存算法版本、置信度、分项原因、成员哈希和人工锁。
- [x] 实现本地事件候选召回与高精度聚类 v1；不依赖 `beauty`、`industry-rules.v1` 或 `is_relevant`。
- [x] 提供默认 dry-run、显式 apply 的聚类命令；重复执行保持幂等，不删除文章或严格重复证据。
- [x] 提供事件列表/详情 API，并补充迁移、算法、幂等、跨领域和人工锁测试。
- [x] 基于当前数据库最新内容和最新聚类结果生成自包含 HTML 每日报告；不编造数据，缺失项明确说明。
- [x] 视觉方向确认后完成日报页面；验证桌面、移动、打印、交互和无障碍基本状态。
- [x] 运行 Ruff、后端测试、Alembic check、空库迁移、SQLite 完整性检查和前端检查。
- [x] 更新 `进度与注意事项.md`、README，并在下方补 Review。

## Review：通用事件聚类 v1 + 最新数据每日报告

- 参考应用正文因账号登录不可见；用户随后提供完整长图，最终按其编辑简报气质实现，没有猜测隐藏页面结构。
- `beauty` 只是通过通用同步命令创建的首个领域；事件聚类直接扫描全部 109 条内容，代码和配置均不含美妆词典、品牌或事件类型。
- 真实聚类比较 1856 对候选，形成 109 个单成员事件、0 个自动多成员事件、0 个灰区候选；宁可保持独立，不为演示制造误并。
- 聚类首次落库创建 109 个事件；输入哈希完善后 run 2 复用全部 109 个事件，下一次 apply 直接复用 run 2，证明稳定 ID 与幂等。
- 现有 109 个历史处理结果投影为 `beauty`：include 102、exclude 7；重复同步 skipped 109。
- 最新日报按北京时间自动选择 2026-08-27，展示 4 个真实故事、4 个来源，输出 `output/daily/2026-08-27-beauty.html`。
- 浏览器在 1440×900 与 390×844 验收；移动端 `scrollWidth == clientWidth == 390`，无横向溢出；打印样式、焦点样式和 reduced-motion 已包含。
- 设计自评：理念 9、层级 9、工艺 8.5、功能 9、原创性 8；整体 8.7/10。保留刊头、连续编号、来源右对齐与数据口径，未加入无依据图片或装饰。
- Ruff 通过，46 项后端测试通过；Alembic `check` 无新增操作，空库从 `0001` 升至 `0013`；主库 integrity `ok`、foreign key 无违规。
- 数据库迁移前备份：`/tmp/navigate_before_generic_events_20260827.db`。本轮未调用 LLM、红狐或其他付费 API。
# 当前实施：DeepSeek 中文编辑日报

目标：保留已确认的编辑简报视觉系统，使用官方 DeepSeek Flash 将英文及中文来源统一加工为面向中文读者的标题、摘要、看点和本期导语；前台移除 AI/算法说明，以内容、来源和原文证据为主。用户要求不设置 `max_tokens`，请求中将省略该参数。

- [x] 核对 DeepSeek 官方模型标识、JSON 输出和接口约束；密钥不落库、不入日志、不写命令参数。
- [x] 新增通用、版本化的 LLM 加工缓存，保存 subject、input hash、task/prompt/model、结构化输出和 Token 用量。
- [x] 实现 DeepSeek Flash 客户端：非思考模式、JSON 输出、不传 `max_tokens`、超时与有限重试。
- [x] 为每个日报事件生成中文标题、中文摘要与关键看点；再基于当日成稿生成中文导语和内容型副标题。
- [x] 日报 CLI 支持 `--llm`，缓存命中不重复付费；任一必要输出失败时不覆盖上一版 HTML。
- [x] 修改 HTML：移除 AI、算法版本和媒体缺失说明；保留精简日期、来源与原文证据。
- [x] 使用当前最新数据调用正式 API，生成并浏览器复核中文日报。
- [x] 补测试并运行 Ruff、后端测试、Alembic check、空库迁移、SQLite 完整性和前端 ESLint。
- [x] 更新 README、进度文档及本任务 Review。

## Review：DeepSeek 中文编辑日报

- 正式使用 `deepseek-v4-flash` 生成 4 条中文故事与本期导语；请求省略 `max_tokens`，关闭思考模式并使用 JSON 输出。
- 一份日报只调用一次模型。成功结果按输入哈希缓存，本次最终 HTML 由缓存离线重建，未再次消耗 Token；记录用量为 prompt 17,176、completion 2,096、合计 19,272。
- 数字事实校验支持中英文单位等值转换（如 billion/亿美元、million/万、M/B 缩写）；故事与来源引用必须与输入完全对应。
- 最终页面包含 4 条中文标题、摘要和关键看点、4 个原文入口；移除了 AI、算法版本、媒体缺失与事件 ID 等开发说明。
- 桌面端与 390×844 手机端均无横向溢出；浏览器验收后已标记为可交付页面。
- Ruff 与 49 项后端测试通过；Alembic `check` 无漂移，空库可从 `0001` 升至 `0014`；主库 integrity/foreign key 与前端 ESLint 均通过。

# 当前实施：D-1 两阶段智能编排日报

目标：日报只使用北京时间前一自然日的 `published_at` 内容；单篇生成可复用的中文标题、逐句摘要与规范标签，事件折叠后由 LLM 基于版本化领域规则完成栏目选择和整期排序；HTML 只消费编排契约，不再硬编码“今日重点/快讯”或 800 字阈值。

- [x] 默认覆盖日改为 `issue_date - 1`，增加 `--latest` 预览模式及空日、时区边界测试。
- [x] 定义领域无关的 `content_editorial.zh.v2`、证据引用和缓存指纹。
- [x] 定义版本化领域 `editorial_policy`，栏目与排序规则不进入平台内核。
- [x] 实现单篇/小批次中文加工与标签缓存；内容哈希不变时不重复调用。
- [x] 实现事件候选组装与 `daily_edition.zh.v1` 整期 LLM 编排。
- [x] 机械校验故事完整覆盖、栏目键、排序、引用、数字事实和配置版本。
- [x] HTML 严格按 `sections[].story_refs` 渲染动态栏目，移除正文长度分组。
- [x] 使用 2026-08-27 最新数据低调用次数生成新版日报并浏览器验收。
- [x] 运行 Ruff、后端测试、Alembic check、空库迁移、SQLite 完整性和前端 ESLint。
- [x] 更新 README、进度文档与 Review。

## Review：D-1 两阶段智能编排日报

- 正式日报默认发行日为 2026-08-28、覆盖日为 2026-08-27；`--latest` 只用于预览，空覆盖日不回退。
- 4 篇单篇工件分别缓存中文标题、逐句 claim、规范标签与字段/字符范围证据引用；整期输入不再包含原始全文。
- 美妆领域规则位于 `config/editorial/beauty.v1.json`：8 个标签、5 个候选栏目及版本化布局/排序策略，核心代码不包含美妆栏目。
- 最终整期编排为 3 栏：资本与监管 1 条、公司与品牌 1 条、产品与创新 2 条；4 个故事完整且唯一出现。
- HTML 已移除“今日重点/快讯”和 800 字阈值，按 `sections` 与 `story_refs` 顺序渲染，并展示文章标签。
- 成功整期编排调用为 prompt 3,711、completion 395、合计 4,106 Token；随后 `--llm-cache-only` 命中单篇及整期缓存，没有新增调用。
- 桌面端与 390×844 手机端无横向溢出，3 个栏目、4 条故事及4个原文入口均通过浏览器验收。
- Ruff 与 65 项后端测试通过；Alembic `check` 无漂移，空库从 `0001` 升至 `0015`；主库 integrity/foreign key 与前端 ESLint 均通过。
